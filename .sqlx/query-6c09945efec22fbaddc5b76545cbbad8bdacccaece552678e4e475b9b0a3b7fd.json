{
  "db_name": "PostgreSQL",
  "query": "SELECT CASE WHEN u.latest_readable_at >= now() - interval '24 hours' THEN 'today' WHEN u.latest_readable_at >= now() - interval '7 days'   THEN 'week' ELSE 'earlier' END AS \"bucket!\", count(*) AS \"title_count!\", COALESCE(sum(u.unread_count), 0)::int8 AS \"chapter_count!\" FROM watchlist_entries w JOIN series s ON s.id = w.series_id LEFT JOIN watchlist_unread u ON u.user_id = w.user_id AND u.series_id = w.series_id CROSS JOIN LATERAL ( SELECT COALESCE((array_agg(ss.state <> 'active' OR p.state <> 'active' ORDER BY ss.chapter_count DESC, ss.last_scanned_at DESC NULLS LAST, p.slug))[1], false) AS source_degraded FROM series_sources ss JOIN providers p ON p.id = ss.provider_id WHERE ss.series_id = w.series_id ) src WHERE w.user_id = $1 AND ($2::watch_status IS NULL OR w.status = $2) AND ($3::text IS NULL OR s.canonical_title ILIKE $3 OR EXISTS (SELECT 1 FROM series_titles st WHERE st.series_id = w.series_id AND st.title ILIKE $3) OR EXISTS (SELECT 1 FROM series_tags stg JOIN tags t ON t.id = stg.tag_id WHERE stg.series_id = w.series_id AND t.name ILIKE $3) OR EXISTS (SELECT 1 FROM series_authors sa JOIN authors a ON a.id = sa.author_id WHERE sa.series_id = w.series_id AND a.name ILIKE $3)) AND (NOT $4::boolean OR COALESCE(u.unread_count, 0) > 0) AND ($5::timestamptz IS NULL OR u.latest_readable_at >= $5) AND (NOT $6::boolean OR src.source_degraded) AND ($7::uuid IS NULL OR w.series_id = $7) GROUP BY 1",
  "describe": {
    "columns": [
      {
        "ordinal": 0,
        "name": "bucket!",
        "type_info": "Text",
        "origin": "Expression"
      },
      {
        "ordinal": 1,
        "name": "title_count!",
        "type_info": "Int8",
        "origin": "Expression"
      },
      {
        "ordinal": 2,
        "name": "chapter_count!",
        "type_info": "Int8",
        "origin": "Expression"
      }
    ],
    "parameters": {
      "Left": [
        "Uuid",
        {
          "Custom": {
            "name": "watch_status",
            "kind": {
              "Enum": [
                "reading",
                "planned",
                "completed",
                "dropped",
                "paused"
              ]
            }
          }
        },
        "Text",
        "Bool",
        "Timestamptz",
        "Bool",
        "Uuid"
      ]
    },
    "nullable": [
      null,
      null,
      null
    ]
  },
  "hash": "6c09945efec22fbaddc5b76545cbbad8bdacccaece552678e4e475b9b0a3b7fd"
}
