{
  "components": {
    "schemas": {
      "AccountSettings": {
        "description": "An account's persisted automatic-sync settings (design v2 §B.6/§B.8).",
        "properties": {
          "auto_sync_enabled": {
            "description": "Whether the background engine syncs this account without being asked.",
            "type": "boolean"
          },
          "conflict_policy": {
            "$ref": "#/components/schemas/ConflictPolicy",
            "description": "How to settle a local/remote disagreement."
          },
          "linked": {
            "description": "Whether a linked external account exists; the other fields are inert without one.",
            "type": "boolean"
          },
          "pending_conflicts": {
            "description": "Conflicts awaiting the user's decision, i.e. the badge count on the Sync panel.",
            "format": "int64",
            "type": "integer"
          }
        },
        "required": [
          "linked",
          "auto_sync_enabled",
          "conflict_policy",
          "pending_conflicts"
        ],
        "type": "object"
      },
      "AccountStatus": {
        "description": "Whether an account may authenticate.\n\nSuspension is deliberately *not* a permission: permissions answer \"what may this\nprincipal do\", and a suspended account is one that may not act at all, including on\nits own data. Modelling it as the absence of every permission would still let the\naccount read its watchlist and refresh its session, which is not what suspension\nmeans. It is therefore an identity-level state checked before authorization runs.",
        "enum": [
          "active",
          "suspended"
        ],
        "type": "string"
      },
      "AdapterKind": {
        "description": "Which adapter implementation drives a provider.",
        "enum": [
          "madara",
          "generic_config",
          "custom"
        ],
        "type": "string"
      },
      "AdminAccountRow": {
        "description": "One row of the admin Sync console's \"Linked accounts\" table. The automatic-sync policy\ncolumns and pending-conflict count (design v2 §B.7) are read-only operator visibility —\nthey are user settings, never operator-overridable.",
        "properties": {
          "auto_sync_enabled": {
            "type": "boolean"
          },
          "conflict_policy": {
            "type": "string"
          },
          "created_at": {
            "type": "string"
          },
          "external_username": {
            "type": [
              "string",
              "null"
            ]
          },
          "last_error": {
            "type": [
              "string",
              "null"
            ]
          },
          "last_synced_at": {
            "type": [
              "string",
              "null"
            ]
          },
          "pending_conflicts": {
            "format": "int64",
            "type": "integer"
          },
          "provider": {
            "type": "string"
          },
          "user_id": {
            "format": "uuid",
            "type": "string"
          },
          "username": {
            "type": "string"
          }
        },
        "required": [
          "user_id",
          "username",
          "provider",
          "auto_sync_enabled",
          "conflict_policy",
          "pending_conflicts",
          "created_at"
        ],
        "type": "object"
      },
      "AdminMappingRow": {
        "description": "One row of the admin Sync console's \"Series mappings\" table.",
        "properties": {
          "external_id": {
            "type": "string"
          },
          "provider": {
            "type": "string"
          },
          "series_id": {
            "format": "uuid",
            "type": "string"
          },
          "series_title": {
            "type": "string"
          },
          "updated_at": {
            "type": "string"
          }
        },
        "required": [
          "series_id",
          "series_title",
          "provider",
          "external_id",
          "updated_at"
        ],
        "type": "object"
      },
      "AdminProfileUpdate": {
        "properties": {
          "email": {
            "type": [
              "string",
              "null"
            ]
          },
          "username": {
            "type": [
              "string",
              "null"
            ]
          }
        },
        "type": "object"
      },
      "AdminRequestRow": {
        "description": "A queue entry as the operator sees it: the subject's identity (while they still exist) and\nwho is handling it.\n\nThe subject-facing fields are a nested `request` rather than `#[serde(flatten)]`. Flattening\nreads better on the wire, but `utoipa` cannot describe it: a flattened field contributes no\nproperties to the generated schema, so the typed client ended up with a struct missing every\nfield the queue actually renders. A nested object is honest about its shape and survives\ncode generation.",
        "properties": {
          "claimed_by": {
            "description": "Operator who claimed it, if any.",
            "type": [
              "string",
              "null"
            ]
          },
          "email": {
            "type": [
              "string",
              "null"
            ]
          },
          "needs_export": {
            "description": "Whether fulfilling this request means disclosing the subject's data export, i.e.\nwhether the queue should offer the \"release export\" action on this row.\n\nDerived server-side from `RequestKind::needs_export` for the same reason `overdue` is\ncomputed in SQL: the console used to re-derive it from `kind` in its own `match`\n(FRONTEND F10), so the set of kinds that disclose an export lived in two places with\nnothing connecting them — and the one the operator sees is the one that decides which\nbutton appears. The server refuses the call either way, so the divergence would have\nshown as a button that does nothing rather than as a disclosure; still a bug, and one\nno test could reach.",
            "type": "boolean"
          },
          "overdue": {
            "description": "Whether the Art. 12(3) deadline has passed with the request still open. Computed in\nSQL against `now()` so the queue cannot disagree with itself about what is late\ndepending on when a client's clock says it rendered.",
            "type": "boolean"
          },
          "request": {
            "$ref": "#/components/schemas/RequestRow"
          },
          "resolved_by": {
            "description": "Operator who resolved it, if resolved.",
            "type": [
              "string",
              "null"
            ]
          },
          "user_id": {
            "description": "The subject's id, or `None` once they have been erased.",
            "type": [
              "string",
              "null"
            ]
          },
          "username": {
            "description": "The subject's username. `None` means the account is gone — for a completed erasure\nthat is the expected end state, not missing data.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "request",
          "overdue",
          "needs_export"
        ],
        "type": "object"
      },
      "AssignRemoteEntry": {
        "properties": {
          "external_id": {
            "type": "string"
          },
          "provider": {
            "type": "string"
          },
          "series_id": {
            "$ref": "#/components/schemas/SeriesId"
          },
          "user_id": {
            "$ref": "#/components/schemas/UserId"
          }
        },
        "required": [
          "user_id",
          "provider",
          "external_id",
          "series_id"
        ],
        "type": "object"
      },
      "AuditView": {
        "description": "One privileged-action record enriched with the actor's username, for the console feed.",
        "properties": {
          "action": {
            "type": "string"
          },
          "actor": {
            "description": "Actor username (`None` for system-originated actions or a since-deleted user).",
            "type": [
              "string",
              "null"
            ]
          },
          "created_at": {
            "type": "string"
          },
          "detail": {
            "$ref": "#/components/schemas/Value"
          },
          "id": {
            "format": "uuid",
            "type": "string"
          },
          "target": {
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "id",
          "action",
          "detail",
          "created_at"
        ],
        "type": "object"
      },
      "Author": {
        "description": "An author/artist credit.",
        "properties": {
          "id": {
            "$ref": "#/components/schemas/AuthorId"
          },
          "name": {
            "type": "string"
          },
          "slug": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "slug",
          "name"
        ],
        "type": "object"
      },
      "AuthorId": {
        "description": "Identifies an author/artist credit.",
        "format": "uuid",
        "type": "string"
      },
      "AuthorizeUrl": {
        "description": "The provider's OAuth consent URL, for `GET /v1/me/sync/{provider}/authorize`.",
        "properties": {
          "url": {
            "type": "string"
          }
        },
        "required": [
          "url"
        ],
        "type": "object"
      },
      "BrowserEmulation": {
        "description": "Which browser the fetch stack impersonates at the TLS/HTTP2 layer.\n\nProviders sit behind Cloudflare/DDoS-Guard, which fingerprint the TLS `ClientHello` and\nthe HTTP/2 SETTINGS frame and compare them against the `User-Agent` header. A mismatch\nis a stronger signal than an unknown client, so the whole profile — cipher suites,\nextension order, ALPS, header order and casing, and the user-agent itself — is picked as\none unit from a browser family rather than assembled field by field.\n\nFamilies, not versions: the concrete build is resolved to whatever the emulation\ncatalogue currently considers newest, so bumping the catalogue does not require a\ndatabase migration or an API-schema change.",
        "enum": [
          "chrome",
          "firefox",
          "safari",
          "edge",
          "ok_http"
        ],
        "type": "string"
      },
      "BulkResult": {
        "description": "Which ids a bulk operation actually acted on, and which it did not.\n\nPer-id rather than a count, because the two failure modes are different and the UI has to\ntell them apart: `skipped` means the caller asked about a series it does not track (a stale\nclient, or a title removed in another tab), which is worth reporting, while a transport\nfailure fails the whole call.",
        "properties": {
          "applied": {
            "items": {
              "$ref": "#/components/schemas/SeriesId"
            },
            "type": "array"
          },
          "skipped": {
            "items": {
              "$ref": "#/components/schemas/SeriesId"
            },
            "type": "array"
          }
        },
        "required": [
          "applied",
          "skipped"
        ],
        "type": "object"
      },
      "Capabilities": {
        "description": "The caller's capabilities and the deployment's enabled features.",
        "properties": {
          "features": {
            "description": "Every feature switched on for this deployment, in registry order.\n\nSent as the *enabled* list rather than a key→bool map: the client asks \"may I show\nthis\", which is a membership test, and a map would invite it to distinguish\n\"explicitly false\" from \"absent\" — a distinction that exists in the control plane's\noverride table and nowhere else.",
            "items": {
              "$ref": "#/components/schemas/Feature"
            },
            "type": "array"
          },
          "permissions": {
            "description": "The permissions this caller currently holds, in registry order.",
            "items": {
              "$ref": "#/components/schemas/Permission"
            },
            "type": "array"
          }
        },
        "required": [
          "permissions",
          "features"
        ],
        "type": "object"
      },
      "ChapterDto": {
        "properties": {
          "number": {
            "format": "double",
            "type": "number"
          },
          "published_at": {
            "type": [
              "string",
              "null"
            ]
          },
          "read": {
            "description": "Whether the requesting user has read this chapter (number ≤ their progress).\n`None` for anonymous requests; `Some(bool)` when a valid `Bearer` token is present\n(frontend §9.2 auth-scoped read-state).",
            "type": [
              "boolean",
              "null"
            ]
          },
          "title": {
            "type": [
              "string",
              "null"
            ]
          },
          "url": {
            "description": "Resolved absolute URL to open the chapter page on the provider.",
            "type": "string"
          }
        },
        "required": [
          "number",
          "url"
        ],
        "type": "object"
      },
      "ChapterId": {
        "description": "Identifies a [`crate::entities::Chapter`].",
        "format": "uuid",
        "type": "string"
      },
      "ChapterRead": {
        "properties": {
          "read": {
            "type": "boolean"
          }
        },
        "required": [
          "read"
        ],
        "type": "object"
      },
      "ConflictPolicy": {
        "description": "How to settle a local/remote disagreement when a series exists on both sides\n(design v2 §B.3).\n\n# Why this is here rather than in `services/sync`\n\nIt was a `pub(crate)` enum in the sync service and a **bare string** on the wire, which put\nthe vocabulary in three places that nothing connected: the service's enum, a prose list in\nthis file's doc comment, and a closed enumeration the frontend maintained by hand\n(FRONTEND F10). A policy added to the service would have compiled everywhere and then\nsilently failed to appear in the picker; a token misspelled in the frontend would have been\nrejected by the service at the far end of a round trip, if at all. Declaring it once here —\nwhere `utoipa` publishes it, `progenitor` generates it and the frontend consumes the\ngenerated form — makes the compiler the connection in both directions.\n\nThe JSON representation is unchanged: `snake_case`, the same four tokens the wire always\ncarried. What changed is that the *schema* now says so, so an unknown token is a `422` at\nthe edge instead of a value that reaches the merge and is quietly read as `newest_wins`.",
        "enum": [
          "local_wins",
          "remote_wins",
          "newest_wins",
          "ask_me"
        ],
        "type": "string"
      },
      "ConflictRow": {
        "description": "One pending conflict awaiting the user's decision (design v2 §B.6 `GET /v1/me/sync/conflicts`).\n\nProduced by `services/sync` from a repository row and re-published verbatim by\n`services/api`. It lives here rather than on the row struct for the reason given in\n[`crate::admin`]: a `SELECT` column rename must not be able to rewrite the public API\nwithout a compile error. The published component name is pinned to `ConflictRow` — the\nmove is an internal layering fix and must not rename anything on the wire.",
        "properties": {
          "detected_at": {
            "type": "string"
          },
          "field": {
            "description": "Which tracked field disagrees, e.g. `progress` or `status`.",
            "type": "string"
          },
          "id": {
            "format": "uuid",
            "type": "string"
          },
          "local_value": {
            "type": "string"
          },
          "provider": {
            "type": "string"
          },
          "remote_value": {
            "type": "string"
          },
          "series_id": {
            "format": "uuid",
            "type": "string"
          },
          "series_title": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "series_id",
          "series_title",
          "provider",
          "field",
          "local_value",
          "remote_value",
          "detected_at"
        ],
        "type": "object"
      },
      "ContentType": {
        "description": "The medium/origin classification of a work.",
        "enum": [
          "manga",
          "manhwa",
          "manhua",
          "webtoon",
          "unknown"
        ],
        "type": "string"
      },
      "ContinueItem": {
        "properties": {
          "cover_url": {
            "type": [
              "string",
              "null"
            ]
          },
          "last_read_number": {
            "format": "double",
            "type": "number"
          },
          "next_number": {
            "description": "The lowest unread chapter number above the user's progress, if any.",
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "series_id": {
            "$ref": "#/components/schemas/SeriesId"
          },
          "series_title": {
            "type": "string"
          },
          "unread": {
            "format": "int64",
            "type": "integer"
          }
        },
        "required": [
          "series_id",
          "series_title",
          "last_read_number",
          "unread"
        ],
        "type": "object"
      },
      "CreateProvider": {
        "properties": {
          "adapter": {
            "$ref": "#/components/schemas/AdapterKind"
          },
          "base_url": {
            "type": "string"
          },
          "config": {},
          "name": {
            "type": "string"
          },
          "politeness": {
            "$ref": "#/components/schemas/Politeness"
          },
          "slug": {
            "type": "string"
          }
        },
        "required": [
          "slug",
          "name",
          "base_url",
          "adapter"
        ],
        "type": "object"
      },
      "DeleteAccount": {
        "description": "Confirmation payload for account deletion.\n\nDeletion is irreversible and cascades across every table, so it demands an explicit,\nunambiguous intent rather than being reachable by a stray `DELETE`. Requiring the\nusername back defends against both a mis-clicked button and a forced cross-site\nnavigation, neither of which can supply it.",
        "properties": {
          "confirm_username": {
            "description": "The authenticated user's own username, typed back as confirmation.",
            "type": "string"
          }
        },
        "required": [
          "confirm_username"
        ],
        "type": "object"
      },
      "DeleteUser": {
        "properties": {
          "confirm_username": {
            "description": "The target account's username, typed back as confirmation.",
            "type": "string"
          },
          "reason": {
            "description": "Why the account is being erased. Recorded in the audit trail, which is the only place\nthe reason can survive — the account itself is about to be gone.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "confirm_username"
        ],
        "type": "object"
      },
      "DirectoryPage": {
        "description": "A page of the directory plus the unfiltered-by-page total, so the UI can render\n\"showing 1–25 of 312\" without a second request.",
        "properties": {
          "total": {
            "description": "Total matching the current search, ignoring `limit`/`offset`.",
            "format": "int64",
            "type": "integer"
          },
          "users": {
            "items": {
              "$ref": "#/components/schemas/DirectoryRow"
            },
            "type": "array"
          }
        },
        "required": [
          "users",
          "total"
        ],
        "type": "object"
      },
      "DirectoryRow": {
        "description": "One row of the operator user directory.\n\nCarries a grant *count* rather than the grants themselves: the directory is a list, and a\nuser's actual capabilities are what the detail view is for. The count is enough to answer\n\"which of these accounts are privileged at all\", which is the question the list is scanned\nfor.",
        "properties": {
          "created_at": {
            "type": "string"
          },
          "email": {
            "type": "string"
          },
          "email_verified": {
            "description": "Whether the address has been confirmed. An unverified account that has existed for\nmonths is usually an abandoned registration.",
            "type": "boolean"
          },
          "id": {
            "type": "string"
          },
          "last_login_at": {
            "type": [
              "string",
              "null"
            ]
          },
          "permission_count": {
            "description": "How many permissions this account holds. `0` is an ordinary reader.",
            "format": "int64",
            "type": "integer"
          },
          "status": {
            "$ref": "#/components/schemas/AccountStatus"
          },
          "tracked_count": {
            "description": "How many series the user tracks — the cheapest signal of a real, in-use account.",
            "format": "int64",
            "type": "integer"
          },
          "username": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "email",
          "username",
          "status",
          "email_verified",
          "permission_count",
          "tracked_count",
          "created_at"
        ],
        "type": "object"
      },
      "DismissRequest": {
        "properties": {
          "id": {
            "format": "uuid",
            "type": "string"
          }
        },
        "required": [
          "id"
        ],
        "type": "object"
      },
      "ExtendRequest": {
        "properties": {
          "due_at": {
            "description": "The new deadline, RFC 3339.",
            "type": "string"
          },
          "reason": {
            "description": "Why an extension is needed. Art. 12(3) requires the subject to be told of the extension\nand its reasons, so recording one is part of taking it.",
            "type": "string"
          }
        },
        "required": [
          "due_at",
          "reason"
        ],
        "type": "object"
      },
      "FailedTaskView": {
        "description": "A failed scan task enriched with its run's provider + mode, for the console error feed.",
        "properties": {
          "attempts": {
            "format": "int32",
            "type": "integer"
          },
          "error": {
            "type": [
              "string",
              "null"
            ]
          },
          "finished_at": {
            "type": [
              "string",
              "null"
            ]
          },
          "id": {
            "format": "uuid",
            "type": "string"
          },
          "kind": {
            "type": "string"
          },
          "mode": {
            "type": "string"
          },
          "provider_slug": {
            "description": "Provider slug of the owning run (`None` if the provider was since deleted).",
            "type": [
              "string",
              "null"
            ]
          },
          "run_id": {
            "format": "uuid",
            "type": "string"
          }
        },
        "required": [
          "id",
          "run_id",
          "mode",
          "kind",
          "attempts"
        ],
        "type": "object"
      },
      "Feature": {
        "description": "A switchable product capability.",
        "enum": [
          "catalogue.browse",
          "catalogue.search",
          "catalogue.recommendations",
          "accounts.registration",
          "accounts.password_reset",
          "accounts.email_verification",
          "accounts.profile",
          "accounts.sessions",
          "accounts.passkeys",
          "privacy.self_export",
          "privacy.self_erasure",
          "privacy.requests",
          "tracking.watchlist",
          "tracking.progress",
          "tracking.feed",
          "tracking.stats",
          "notifications.in_app",
          "notifications.live",
          "notifications.preferences",
          "notifications.email",
          "notifications.webhook",
          "notifications.discord",
          "sync.external",
          "sync.auto_push",
          "sync.scheduled_pull",
          "sync.conflict_review",
          "sync.history",
          "scanning.scheduler",
          "scanning.manual",
          "scanning.full",
          "scanning.merge_queue",
          "scanning.auto_merge",
          "admin.providers",
          "admin.adapter_test",
          "admin.sync",
          "admin.audit",
          "admin.stats",
          "admin.users",
          "admin.feature_flags"
        ],
        "type": "string"
      },
      "FeatureGroup": {
        "description": "The control plane's grouping of features into sections.",
        "enum": [
          "catalogue",
          "accounts",
          "privacy",
          "tracking",
          "notifications",
          "sync",
          "scanning",
          "operations"
        ],
        "type": "string"
      },
      "FeedEntry": {
        "properties": {
          "chapter_number": {
            "format": "double",
            "type": "number"
          },
          "chapter_title": {
            "type": [
              "string",
              "null"
            ]
          },
          "discovered_at": {
            "type": "string"
          },
          "provider_slug": {
            "type": "string"
          },
          "series_id": {
            "$ref": "#/components/schemas/SeriesId"
          },
          "series_title": {
            "type": "string"
          },
          "url": {
            "description": "Ready-to-open absolute URL, resolved from the provider base + relative path.",
            "type": "string"
          }
        },
        "required": [
          "series_id",
          "series_title",
          "chapter_number",
          "provider_slug",
          "url",
          "discovered_at"
        ],
        "type": "object"
      },
      "FeedbackBody": {
        "description": "A reader's verdict on a recommendation.",
        "properties": {
          "verdict": {
            "description": "`not_interested` (decays) or `hide_forever` (does not).",
            "type": "string"
          }
        },
        "required": [
          "verdict"
        ],
        "type": "object"
      },
      "FlagView": {
        "description": "One feature as the control plane shows it.\n\nThe four booleans are not a struct that wants splitting: they answer four different\nquestions the page asks side by side — is it on, is that the shipped value, did someone\ndecide it, and can it be changed at all — and grouping any of them would only move the\nflattening into the client.",
        "properties": {
          "default_enabled": {
            "description": "What this feature ships as, so the page can show \"changed from default\" without the\nclient hard-coding a copy of the registry.",
            "type": "boolean"
          },
          "description": {
            "description": "What switching it off actually does, written to be read immediately before someone\nflips a production switch.",
            "type": "string"
          },
          "enabled": {
            "description": "The effective state: the override if there is one, else the shipped default.",
            "type": "boolean"
          },
          "group": {
            "$ref": "#/components/schemas/FeatureGroup"
          },
          "key": {
            "$ref": "#/components/schemas/Feature"
          },
          "locked": {
            "description": "Whether the feature refuses to be switched off — the deployment's recovery paths. The\nUI disables the control; the API refuses the write regardless.",
            "type": "boolean"
          },
          "note": {
            "description": "Why the switch was last flipped, if the operator said.",
            "type": [
              "string",
              "null"
            ]
          },
          "overridden": {
            "description": "Whether an operator has explicitly decided this one. `false` means it is following the\nshipped default.",
            "type": "boolean"
          },
          "title": {
            "type": "string"
          },
          "updated_at": {
            "description": "When it was last changed. Absent while the feature is at its default.",
            "type": [
              "string",
              "null"
            ]
          },
          "updated_by": {
            "description": "Username of the operator who last changed it; `None` once that account is erased.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "key",
          "group",
          "title",
          "description",
          "enabled",
          "default_enabled",
          "overridden",
          "locked"
        ],
        "type": "object"
      },
      "ForgotPasswordRequest": {
        "properties": {
          "email": {
            "description": "The account's email address. Unknown addresses are accepted silently.",
            "type": "string"
          }
        },
        "required": [
          "email"
        ],
        "type": "object"
      },
      "FulfilErasure": {
        "properties": {
          "confirm_username": {
            "description": "The subject's username, typed back as confirmation.",
            "type": "string"
          }
        },
        "required": [
          "confirm_username"
        ],
        "type": "object"
      },
      "GrantRow": {
        "description": "A single grant, with its provenance, for the user-detail view.",
        "properties": {
          "granted_at": {
            "type": "string"
          },
          "granted_by": {
            "description": "Who granted it, or `None` for a grant made by the migration from the old role model\nor by an administrator since erased.",
            "type": [
              "string",
              "null"
            ]
          },
          "known": {
            "description": "Whether this build recognises the token. `false` means the grant is inert.",
            "type": "boolean"
          },
          "permission": {
            "description": "The permission token. A string rather than the enum because a row surviving from a\nbuild that had a capability this one does not must still be *visible* to an\nadministrator — that is precisely when they need to see and remove it.",
            "type": "string"
          }
        },
        "required": [
          "permission",
          "known",
          "granted_at"
        ],
        "type": "object"
      },
      "HistoryRow": {
        "description": "One row of the user-facing sync history (design v2 §B.6 `GET /v1/me/sync/history`).\nSee [`ConflictView`] for why it lives here.",
        "properties": {
          "action": {
            "description": "What the engine did, e.g. `pull`, `push` or `resolve`.",
            "type": "string"
          },
          "created_at": {
            "type": "string"
          },
          "detail": {
            "description": "Free-form, action-specific detail (the changed field and its before/after values).",
            "type": "object"
          },
          "id": {
            "format": "uuid",
            "type": "string"
          },
          "provider": {
            "type": "string"
          },
          "series_id": {
            "format": "uuid",
            "type": "string"
          },
          "series_title": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "series_id",
          "series_title",
          "provider",
          "action",
          "detail",
          "created_at"
        ],
        "type": "object"
      },
      "KeyRebuildView": {
        "description": "What a normalized-key rebuild changed.\n\n`normalized_title` is a persisted matching key, so a change to the normalization rules only\nreaches rows that happen to be re-scanned until this runs.",
        "properties": {
          "series_scanned": {
            "format": "int64",
            "type": "integer"
          },
          "series_updated": {
            "format": "int64",
            "type": "integer"
          },
          "titles_deduplicated": {
            "description": "Alternative titles dropped because the corrected rules collapsed them onto a key the\nsame series already held.",
            "format": "int64",
            "type": "integer"
          },
          "titles_scanned": {
            "format": "int64",
            "type": "integer"
          },
          "titles_updated": {
            "format": "int64",
            "type": "integer"
          }
        },
        "required": [
          "series_scanned",
          "series_updated",
          "titles_scanned",
          "titles_updated",
          "titles_deduplicated"
        ],
        "type": "object"
      },
      "LegalDocumentView": {
        "description": "One document, in the locale that was actually served.",
        "properties": {
          "body": {
            "type": "string"
          },
          "format": {
            "description": "Always `markdown`. Present so a future format is a value rather than a new endpoint.",
            "type": "string"
          },
          "locale": {
            "description": "The locale served, which is **not** necessarily the one requested — a reader asking for\nGerman and receiving the only available English text has to be told, or they conclude\nthe operator writes German like that.",
            "type": "string"
          },
          "slug": {
            "type": "string"
          },
          "title": {
            "type": [
              "string",
              "null"
            ]
          },
          "updated": {
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "slug",
          "locale",
          "format",
          "body"
        ],
        "type": "object"
      },
      "LegalIndexEntry": {
        "description": "One entry in the index the footer renders.",
        "properties": {
          "kind": {
            "$ref": "#/components/schemas/LegalKind",
            "description": "`inline` for a document this API serves, `external` for one hosted elsewhere."
          },
          "locales": {
            "description": "The locales an `inline` document is published in.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "slug": {
            "description": "The URL slug, and the key an operator configured this document under.",
            "type": "string"
          },
          "title": {
            "description": "The operator's title for the requested locale, when they set one.",
            "type": [
              "string",
              "null"
            ]
          },
          "updated": {
            "description": "The \"last updated\" line, verbatim as configured.",
            "type": [
              "string",
              "null"
            ]
          },
          "url": {
            "description": "Where to send the reader, for an `external` document.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "slug",
          "kind",
          "locales"
        ],
        "type": "object"
      },
      "LegalKind": {
        "description": "Whether a document is served from here or hosted elsewhere.",
        "enum": [
          "inline",
          "external"
        ],
        "type": "string"
      },
      "LoginRequest": {
        "properties": {
          "login": {
            "description": "Email or username.",
            "type": "string"
          },
          "password": {
            "type": "string"
          }
        },
        "required": [
          "login",
          "password"
        ],
        "type": "object"
      },
      "MarkRead": {
        "properties": {
          "all": {
            "description": "Mark the caller's whole inbox read, rather than the listed ids.\n\nA client only ever holds the page it loaded, so \"mark all read\" sent as a list of ids\nmarks one page and quietly leaves the rest unread.",
            "type": "boolean"
          },
          "ids": {
            "description": "The notifications to mark read. Ignored when `all` is set.",
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "MarkReadTo": {
        "properties": {
          "number": {
            "format": "double",
            "type": "number"
          }
        },
        "required": [
          "number"
        ],
        "type": "object"
      },
      "MeStats": {
        "description": "Lifetime reading stats for the Home/Profile headline (frontend §9.3 `GET /v1/me/stats`).\n`chapters_read` is the sum of whole chapters below each series' last-read marker — an\nhonest proxy over stored progress (there is no per-chapter read-event log, so a daily\n\"streak\" is intentionally omitted rather than fabricated).",
        "properties": {
          "chapters_read": {
            "format": "int64",
            "type": "integer"
          },
          "completed": {
            "format": "int64",
            "type": "integer"
          },
          "reading": {
            "format": "int64",
            "type": "integer"
          },
          "tracking": {
            "format": "int64",
            "type": "integer"
          },
          "unread": {
            "format": "int64",
            "type": "integer"
          }
        },
        "required": [
          "tracking",
          "reading",
          "completed",
          "chapters_read",
          "unread"
        ],
        "type": "object"
      },
      "MergeCandidateView": {
        "description": "A pending merge candidate, enriched with everything the console needs to triage it without\nopening both series (design §11 `GET /v1/admin/merge-candidates`).\n\nThe counts and `suggested_keep` are not decoration. Acting on a candidate means choosing\nwhich of the two rows survives, and the absorbed id *stops existing* — every bookmark,\nnotification and external tracker mapping naming it breaks. The right answer is whichever\nseries carries more of the catalogue, which is a comparison the server can make once instead\nof an operator eyeballing it per row.",
        "properties": {
          "candidate_chapters": {
            "format": "int64",
            "type": "integer"
          },
          "candidate_id": {
            "$ref": "#/components/schemas/SeriesId"
          },
          "candidate_sources": {
            "format": "int64",
            "type": "integer"
          },
          "candidate_title": {
            "type": "string"
          },
          "created_at": {
            "type": "string"
          },
          "id": {
            "format": "uuid",
            "type": "string"
          },
          "reason": {
            "type": [
              "string",
              "null"
            ]
          },
          "score": {
            "format": "float",
            "type": "number"
          },
          "series_chapters": {
            "format": "int64",
            "type": "integer"
          },
          "series_id": {
            "$ref": "#/components/schemas/SeriesId"
          },
          "series_sources": {
            "format": "int64",
            "type": "integer"
          },
          "series_title": {
            "type": "string"
          },
          "signals": {
            "description": "Stable slugs for the scoring rules that fired — `exact_title`, `compact_identity`,\n`alias_identity`, `near_identical`, `shared_author`, and so on. Rendered as badges; the\nset is `tankovault_domain::matching::MatchSignals::labels`.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "suggested_keep": {
            "$ref": "#/components/schemas/SeriesId",
            "description": "Which side the console should offer to keep. Advisory: the merge endpoint takes an\nexplicit direction."
          },
          "updated_at": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "series_id",
          "series_title",
          "series_sources",
          "series_chapters",
          "candidate_id",
          "candidate_title",
          "candidate_sources",
          "candidate_chapters",
          "score",
          "signals",
          "suggested_keep",
          "created_at",
          "updated_at"
        ],
        "type": "object"
      },
      "MergeRequest": {
        "properties": {
          "keep": {
            "$ref": "#/components/schemas/SeriesId",
            "description": "The surviving canonical series."
          },
          "merge": {
            "$ref": "#/components/schemas/SeriesId",
            "description": "The series merged into `keep` and then deleted."
          }
        },
        "required": [
          "keep",
          "merge"
        ],
        "type": "object"
      },
      "MergeSweepView": {
        "description": "What one duplicate-reconciliation sweep did.\n\nReturned by `POST /v1/admin/merge-candidates/sweep` and logged by the scheduled sweep, so an\noperator can see the effect of a threshold change on a single run before leaving it to the\nschedule.",
        "properties": {
          "auto_merged": {
            "description": "Pairs merged without asking, because a structural identity rule fired *and* the score\ncleared the automatic-merge threshold.",
            "format": "int64",
            "type": "integer"
          },
          "deferred": {
            "description": "Pairs skipped because the sweep's per-run automatic-merge budget was exhausted. Non-zero\nmeans the next sweep has more to do, not that anything failed.",
            "format": "int64",
            "type": "integer"
          },
          "distinct": {
            "description": "Pairs the sweep judged distinct and left alone.",
            "format": "int64",
            "type": "integer"
          },
          "pairs_examined": {
            "description": "Pairs re-scored. The shortlist is produced by blocking on the whitespace-insensitive\ntitle key, so this is far smaller than the number of series.",
            "format": "int64",
            "type": "integer"
          },
          "queued": {
            "description": "Pairs the sweep put in the review queue that had never been there.\n\nCounted apart from `requeued` because only these lengthen the queue: the sweep re-scores\nrows that are already open on every run, and folding those into one \"queued\" number\nreports a queue growing by hundreds when it grew by tens. Net change in queue length is\n`queued + reopened - withdrawn`.",
            "format": "int64",
            "type": "integer"
          },
          "reopened": {
            "description": "Pairs the scorer had closed as distinct that enrichment has brought back into review.",
            "format": "int64",
            "type": "integer"
          },
          "requeued": {
            "description": "Rows already open that this sweep re-scored in place. The queue is no longer for these.",
            "format": "int64",
            "type": "integer"
          },
          "withdrawn": {
            "description": "Open queue rows removed because re-scoring with everything now known about both series\nput the pair below the review floor.",
            "format": "int64",
            "type": "integer"
          }
        },
        "required": [
          "pairs_examined",
          "auto_merged",
          "queued",
          "requeued",
          "reopened",
          "withdrawn",
          "distinct",
          "deferred"
        ],
        "type": "object"
      },
      "NewPrivacyRequest": {
        "properties": {
          "detail": {
            "description": "What the subject is asking for, in their words. Required for rectification — \"correct\nmy data\" without saying which field to what is not actionable — and optional otherwise.",
            "type": [
              "string",
              "null"
            ]
          },
          "kind": {
            "$ref": "#/components/schemas/RequestKind",
            "description": "Which right is being exercised."
          }
        },
        "required": [
          "kind"
        ],
        "type": "object"
      },
      "NextUnread": {
        "description": "The chapter a `Continue` button opens, and what the ledger's `Next unread` column shows.",
        "properties": {
          "number": {
            "description": "The chapter number, parts included.",
            "format": "double",
            "type": "number"
          },
          "released_at": {
            "type": "string"
          },
          "title": {
            "description": "The provider's chapter title, when it publishes one.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "number",
          "released_at"
        ],
        "type": "object"
      },
      "NotificationId": {
        "description": "Identifies a notification.",
        "format": "uuid",
        "type": "string"
      },
      "NotificationsView": {
        "properties": {
          "items": {
            "description": "One page of the inbox, newest first. Free-form per notification kind: the notifier writes\nan open `payload`, and pinning a schema here would drop every kind it has not shipped yet.",
            "items": {},
            "type": "array"
          },
          "total": {
            "description": "Notifications the caller has, in total — the pager's denominator.",
            "format": "int64",
            "type": "integer"
          },
          "unread": {
            "description": "Of those, how many are unread. Counted server-side on purpose: derived from `items` it is\nonly ever the unread count *of the loaded page*, which is what pinned the bell at 100.",
            "format": "int64",
            "type": "integer"
          }
        },
        "required": [
          "items",
          "total",
          "unread"
        ],
        "type": "object"
      },
      "PasskeyChallenge": {
        "description": "The challenge, plus the handle the client echoes back to complete it.\n\n`options` is a W3C `PublicKeyCredentialRequestOptions` envelope, passed to\n`navigator.credentials.get()` verbatim. It is typed as an opaque JSON document here on\npurpose: the shape is the `WebAuthn` specification's, not this API's, and mirroring it into\nthe `OpenAPI` document would be this service publishing a second, hand-maintained copy of a\nstandard — the exact drift that made `/v1/me/sync/*` move its DTOs into\n`crates/contracts`. Both ends parse it with the *same* crate (`webauthn-rs-proto`, pinned to\none version in both workspaces), so there is nothing for the two to disagree about.",
        "properties": {
          "ceremony_id": {
            "description": "Opaque handle for this ceremony. Return it with the assertion.",
            "format": "uuid",
            "type": "string"
          },
          "options": {
            "description": "A W3C `PublicKeyCredentialRequestOptions` envelope. Hand it to the browser unmodified.",
            "type": "object"
          }
        },
        "required": [
          "ceremony_id",
          "options"
        ],
        "type": "object"
      },
      "PasskeyDto": {
        "description": "A registered passkey as the account page shows it.\n\nNote what is **not** here: the credential itself, the public key and the credential id. None\nof them is a secret, but none is useful to a browser either, and a credential id is the\nlookup key a sign-in resolves an account from — publishing every account's ids on an\nauthenticated page would hand an attacker who compromises one session the material to\nrecognise that user's authenticator elsewhere.",
        "properties": {
          "created_at": {
            "type": "string"
          },
          "id": {
            "format": "uuid",
            "type": "string"
          },
          "label": {
            "type": "string"
          },
          "last_used_at": {
            "description": "When this key was last used to sign in; absent if it never has been. Surfaced so a user\ncan tell a live key from one registered on a laptop they no longer own.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "id",
          "label",
          "created_at"
        ],
        "type": "object"
      },
      "PasskeyLoginRequest": {
        "description": "A signed assertion, returned with the handle from [`PasskeyChallenge`].",
        "properties": {
          "ceremony_id": {
            "format": "uuid",
            "type": "string"
          },
          "credential": {
            "description": "The `PublicKeyCredential` produced by `navigator.credentials.get()`, serialised as the\n`WebAuthn` specification defines it.",
            "type": "object"
          }
        },
        "required": [
          "ceremony_id",
          "credential"
        ],
        "type": "object"
      },
      "PasskeyRegisterChallenge": {
        "description": "The challenge, plus the handle the client echoes back to complete it.\n\n`options` is a W3C `PublicKeyCredentialCreationOptions` envelope. It is an opaque JSON\ndocument here for the reason [`crate::auth::passkey::PasskeyChallenge`] gives: the shape\nbelongs to the `WebAuthn` specification, and both ends parse it with the same crate.",
        "properties": {
          "ceremony_id": {
            "format": "uuid",
            "type": "string"
          },
          "options": {
            "description": "A W3C `PublicKeyCredentialCreationOptions` envelope. Hand it to the browser unmodified.",
            "type": "object"
          }
        },
        "required": [
          "ceremony_id",
          "options"
        ],
        "type": "object"
      },
      "PasskeyRegisterFinish": {
        "description": "The attestation returned by `navigator.credentials.create()`.",
        "properties": {
          "ceremony_id": {
            "format": "uuid",
            "type": "string"
          },
          "credential": {
            "description": "The `RegisterPublicKeyCredential` the browser produced, serialised as the `WebAuthn`\nspecification defines it.",
            "type": "object"
          }
        },
        "required": [
          "ceremony_id",
          "credential"
        ],
        "type": "object"
      },
      "PasskeyRegisterStart": {
        "description": "Proof of the current password, plus what to call the new key.",
        "properties": {
          "current_password": {
            "description": "The caller's current password. Required — see [`passkey_register_start`].",
            "type": "string"
          },
          "label": {
            "description": "What to call the key in the revoke list. Defaults to \"Passkey\" when absent or blank.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "current_password"
        ],
        "type": "object"
      },
      "PasskeyRename": {
        "description": "A new name for an existing key.",
        "properties": {
          "label": {
            "type": "string"
          }
        },
        "required": [
          "label"
        ],
        "type": "object"
      },
      "PasswordChange": {
        "properties": {
          "current_password": {
            "type": "string"
          },
          "new_password": {
            "type": "string"
          }
        },
        "required": [
          "current_password",
          "new_password"
        ],
        "type": "object"
      },
      "Permission": {
        "description": "Everything a principal can be authorized to do.\n\nThe enum is exhaustive by design: a permission that exists only as a string somewhere\ncannot be listed in the admin UI, cannot be spell-checked by the compiler, and cannot be\naudited as a known capability. Adding a capability means adding a variant here and\nnothing else.",
        "enum": [
          "providers.read",
          "providers.write",
          "providers.create",
          "providers.delete",
          "providers.state",
          "providers.test",
          "scans.read",
          "scans.run",
          "merge.read",
          "merge.write",
          "sync.admin.read",
          "sync.admin.write",
          "users.read",
          "users.write",
          "users.permissions",
          "users.delete",
          "users.sessions",
          "privacy.read",
          "privacy.write",
          "privacy.export",
          "system.stats",
          "audit.read",
          "flags.read",
          "flags.write"
        ],
        "type": "string"
      },
      "PermissionCatalogue": {
        "description": "The permission catalogue and its presets.",
        "properties": {
          "permissions": {
            "items": {
              "$ref": "#/components/schemas/PermissionInfo"
            },
            "type": "array"
          },
          "presets": {
            "items": {
              "$ref": "#/components/schemas/PresetInfo"
            },
            "type": "array"
          }
        },
        "required": [
          "permissions",
          "presets"
        ],
        "type": "object"
      },
      "PermissionGroup": {
        "description": "The admin UI's grouping of permissions into sections.",
        "enum": [
          "providers",
          "scanning",
          "catalogue",
          "sync",
          "users",
          "privacy",
          "observability",
          "flags"
        ],
        "type": "string"
      },
      "PermissionInfo": {
        "description": "One entry of the permission catalogue the editor renders.",
        "properties": {
          "description": {
            "description": "What the capability allows, in the operator's terms.",
            "type": "string"
          },
          "group": {
            "$ref": "#/components/schemas/PermissionGroup"
          },
          "key": {
            "$ref": "#/components/schemas/Permission"
          }
        },
        "required": [
          "key",
          "group",
          "description"
        ],
        "type": "object"
      },
      "PermissionPreset": {
        "description": "A named bundle offered in the permission editor as a starting point.\n\nNot persisted and never consulted during an authorization decision — see the module docs\non why this is not a role by another name.",
        "enum": [
          "reader",
          "operator",
          "administrator"
        ],
        "type": "string"
      },
      "Politeness": {
        "description": "Crawl politeness parameters for one provider.\n\n**`rps` and `concurrency` are enforced per worker process, not fleet-wide.** The fetch\nstack is built per provider inside each worker, so the load a provider actually sees is\n`rps × replicas`. Sizing a provider's budget means dividing the intended aggregate by the\nreplica count; scaling workers out without lowering these raises the real crawl rate.\n(A cross-replica token bucket in Redis is the documented follow-up — see\n`crates/fetch/src/ratelimit.rs`.)",
        "properties": {
          "concurrency": {
            "description": "Maximum concurrent requests to this provider, per worker process.",
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "crawl_delay_ms": {
            "description": "Minimum delay between requests, in milliseconds.",
            "format": "int64",
            "minimum": 0,
            "type": "integer"
          },
          "emulation": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/BrowserEmulation",
                "description": "Browser to impersonate, or `None` to crawl as an identifiable bot using `user_agent`."
              }
            ]
          },
          "rps": {
            "description": "Requests per second, per worker process (see the type-level note on fleet totals).",
            "format": "double",
            "type": "number"
          },
          "user_agent": {
            "description": "User-agent sent on ordinary (non-challenge) requests — **only when `emulation` is\n`None`**. An emulated client sends the user-agent belonging to its profile, because\na custom string over a browser TLS fingerprint is exactly the mismatch WAFs look for.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "PresetInfo": {
        "description": "A named starting point in the permission editor.",
        "properties": {
          "key": {
            "$ref": "#/components/schemas/PermissionPreset"
          },
          "permissions": {
            "description": "The permissions this preset expands to. Sent expanded rather than by name so applying a\npreset is a pure client-side operation over the same checklist the operator then edits —\nwhich is what keeps a preset from behaving like a stored role.",
            "items": {
              "$ref": "#/components/schemas/Permission"
            },
            "type": "array"
          }
        },
        "required": [
          "key",
          "permissions"
        ],
        "type": "object"
      },
      "ProblemDetails": {
        "description": "RFC 9457 `application/problem+json` error body shape produced by [`ApiError`]. Declared\npurely for `OpenAPI` documentation — `tankovault_service::problem` builds the JSON, so runtime\ncallers never construct this type. It mirrors `problem::ProblemBody`; a test asserts the two\nagree field for field, since only this copy is published to clients.",
        "example": {
          "detail": "resource not found",
          "status": 404,
          "title": "not_found",
          "type": "about:blank#not_found"
        },
        "properties": {
          "detail": {
            "type": "string"
          },
          "status": {
            "format": "int32",
            "minimum": 0,
            "type": "integer"
          },
          "title": {
            "type": "string"
          },
          "type": {
            "type": "string"
          }
        },
        "required": [
          "type",
          "title",
          "status",
          "detail"
        ],
        "type": "object"
      },
      "ProfileDto": {
        "properties": {
          "email": {
            "type": "string"
          },
          "id": {
            "format": "uuid",
            "type": "string"
          },
          "status": {
            "$ref": "#/components/schemas/AccountStatus",
            "description": "Whether the account is active or suspended. A suspended caller cannot reach this\nendpoint at all, so in practice this is always `active` — it is here because the\nprofile is the account's identity record and omitting its state would make the DTO an\nincomplete picture of it."
          },
          "username": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "email",
          "username",
          "status"
        ],
        "type": "object"
      },
      "ProfileUpdate": {
        "properties": {
          "current_password": {
            "description": "Required when `email` changes the address on the account. See [`patch_profile`].",
            "type": [
              "string",
              "null"
            ]
          },
          "email": {
            "type": [
              "string",
              "null"
            ]
          },
          "username": {
            "type": [
              "string",
              "null"
            ]
          }
        },
        "type": "object"
      },
      "ProgressDto": {
        "properties": {
          "last_read_part_number": {
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "last_read_whole_number": {
            "format": "double",
            "type": "number"
          }
        },
        "required": [
          "last_read_whole_number"
        ],
        "type": "object"
      },
      "ProgressUpdate": {
        "properties": {
          "last_read_whole_number": {
            "description": "The whole-chapter frontier to set outright (design v2 §A.6 — renamed from\n`last_read_number`, same semantics).",
            "format": "double",
            "type": "number"
          }
        },
        "required": [
          "last_read_whole_number"
        ],
        "type": "object"
      },
      "Provider": {
        "description": "A source site. The single place a domain (`base_url`) is defined.",
        "properties": {
          "adapter": {
            "$ref": "#/components/schemas/AdapterKind"
          },
          "base_url": {
            "description": "The domain root. Changing this one field migrates every stored link.",
            "type": "string"
          },
          "config": {
            "description": "Adapter selector/pagination config (shape depends on `adapter`)."
          },
          "created_at": {
            "type": "string"
          },
          "id": {
            "$ref": "#/components/schemas/ProviderId"
          },
          "last_full_scan_at": {
            "description": "Wire shape is a plain string (whatever `time`'s serde impl emits), never parsed\nclient-side — kept untyped here so the generated frontend type doesn't need a date\ncrate (mirrors the `no date crate in the bundle` constraint on the other timestamp\nfields below).",
            "type": [
              "string",
              "null"
            ]
          },
          "name": {
            "type": "string"
          },
          "politeness": {
            "$ref": "#/components/schemas/Politeness"
          },
          "slug": {
            "type": "string"
          },
          "state": {
            "$ref": "#/components/schemas/ProviderState"
          },
          "updated_at": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "slug",
          "name",
          "base_url",
          "adapter",
          "config",
          "state",
          "politeness",
          "created_at",
          "updated_at"
        ],
        "type": "object"
      },
      "ProviderId": {
        "description": "Identifies a [`crate::entities::Provider`].",
        "format": "uuid",
        "type": "string"
      },
      "ProviderInfo": {
        "description": "A registered external provider's identity, as listed by `GET /v1/me/sync/providers`.",
        "properties": {
          "name": {
            "description": "User-facing display name (e.g. `AniList`).",
            "type": "string"
          },
          "slug": {
            "description": "Stable key used in sync paths and mapping keys (e.g. `anilist`).",
            "type": "string"
          }
        },
        "required": [
          "slug",
          "name"
        ],
        "type": "object"
      },
      "ProviderStat": {
        "description": "One row of the per-provider statistics table. Enum columns are text-cast; the provider's\nidentity fields are joined in so the console renders the table from one fetch.",
        "properties": {
          "adapter": {
            "description": "Adapter implementation token (`madara` | `generic_config` | `custom`).",
            "type": "string"
          },
          "blocked_sources": {
            "description": "Source links currently in a non-active state.",
            "format": "int64",
            "type": "integer"
          },
          "chapter_count": {
            "format": "int64",
            "type": "integer"
          },
          "chapters_24h": {
            "format": "int64",
            "type": "integer"
          },
          "chapters_7d": {
            "format": "int64",
            "type": "integer"
          },
          "last_chapter_at": {
            "type": [
              "string",
              "null"
            ]
          },
          "last_full_scan_at": {
            "type": [
              "string",
              "null"
            ]
          },
          "last_run_at": {
            "type": [
              "string",
              "null"
            ]
          },
          "last_run_state": {
            "description": "State of the provider's most recent scan run, if any.",
            "type": [
              "string",
              "null"
            ]
          },
          "last_scanned_at": {
            "type": [
              "string",
              "null"
            ]
          },
          "name": {
            "type": "string"
          },
          "provider_id": {
            "format": "uuid",
            "type": "string"
          },
          "series_count": {
            "description": "Distinct series that have at least one source on this provider.",
            "format": "int64",
            "type": "integer"
          },
          "slug": {
            "type": "string"
          },
          "source_count": {
            "description": "Source links (series ↔ provider joins) this provider owns.",
            "format": "int64",
            "type": "integer"
          },
          "state": {
            "description": "Provider health state token (`active` | `disabled` | `blocked` | …).",
            "type": "string"
          }
        },
        "required": [
          "provider_id",
          "slug",
          "name",
          "state",
          "adapter",
          "series_count",
          "source_count",
          "blocked_sources",
          "chapter_count",
          "chapters_24h",
          "chapters_7d"
        ],
        "type": "object"
      },
      "ProviderState": {
        "description": "Live health state of a provider (drives the circuit breaker + console tiles).",
        "enum": [
          "active",
          "degraded",
          "challenged",
          "solving",
          "blocked",
          "disabled"
        ],
        "type": "string"
      },
      "PublicProvider": {
        "description": "A provider entry for the Discover filter list: identity plus how many distinct series it\ncarries, so the UI can show \"Provider (N)\" options without exposing operator-only\nconfig/politeness.",
        "properties": {
          "id": {
            "format": "uuid",
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "series_count": {
            "description": "Distinct canonical series with at least one source on this provider.",
            "format": "int64",
            "type": "integer"
          },
          "slug": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "slug",
          "name",
          "series_count"
        ],
        "type": "object"
      },
      "Recommendation": {
        "description": "One recommended series and why it is here.",
        "properties": {
          "because_series_id": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/SeriesId",
                "description": "The series that produced this one, when a seed did."
              }
            ]
          },
          "because_title": {
            "type": [
              "string",
              "null"
            ]
          },
          "content_type": {
            "$ref": "#/components/schemas/ContentType"
          },
          "cover_url": {
            "type": [
              "string",
              "null"
            ]
          },
          "score": {
            "format": "float",
            "type": "number"
          },
          "series_id": {
            "$ref": "#/components/schemas/SeriesId"
          },
          "shared": {
            "description": "Features shared with `because_series_id`, most explanatory first.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "source_count": {
            "format": "int64",
            "type": "integer"
          },
          "status": {
            "$ref": "#/components/schemas/SeriesStatus"
          },
          "title": {
            "type": "string"
          }
        },
        "required": [
          "series_id",
          "title",
          "content_type",
          "status",
          "source_count",
          "score",
          "shared"
        ],
        "type": "object"
      },
      "RegisterRequest": {
        "properties": {
          "email": {
            "type": "string"
          },
          "password": {
            "type": "string"
          },
          "username": {
            "type": "string"
          }
        },
        "required": [
          "email",
          "username",
          "password"
        ],
        "type": "object"
      },
      "RegisterResponse": {
        "description": "The result of [`register`]. Registration has two outcomes depending on whether email\ndelivery is configured:\n\n- Email enabled: the account is created **unconfirmed**, a confirmation link is emailed,\n  and no session is issued (`verification_required = true`, `access_token` absent). The\n  user must click the link before they can sign in.\n- Email not configured (dev/self-host without SMTP): confirmation cannot be delivered, so\n  the account is activated immediately and a session is issued exactly like\n  [`super::login::login`] (`verification_required = false`, `access_token` present).",
        "properties": {
          "access_token": {
            "description": "The issued bearer access token — present only when the account was activated\nimmediately (email delivery not configured). Mirrors [`TokenResponse::access_token`].",
            "type": [
              "string",
              "null"
            ]
          },
          "expires_in": {
            "description": "Access-token lifetime in seconds; present exactly when `access_token` is.",
            "format": "int64",
            "type": [
              "integer",
              "null"
            ]
          },
          "verification_required": {
            "description": "`true` when a confirmation email was sent and the account must be verified before it\ncan sign in. When `true`, no session was issued and `access_token` is absent.",
            "type": "boolean"
          }
        },
        "required": [
          "verification_required"
        ],
        "type": "object"
      },
      "RemoteEntryRow": {
        "description": "One row of the admin console's \"Unmatched remote entries\" queue: a fetched provider entry\nthe auto-matcher could not confidently link to a local series.",
        "properties": {
          "content_type": {
            "type": "string"
          },
          "external_id": {
            "type": "string"
          },
          "progress": {
            "format": "double",
            "type": "number"
          },
          "provider": {
            "type": "string"
          },
          "start_year": {
            "format": "int32",
            "type": [
              "integer",
              "null"
            ]
          },
          "status": {
            "type": "string"
          },
          "title": {
            "type": "string"
          },
          "user_id": {
            "format": "uuid",
            "type": "string"
          },
          "username": {
            "type": "string"
          }
        },
        "required": [
          "user_id",
          "username",
          "provider",
          "external_id",
          "title",
          "status",
          "progress",
          "content_type"
        ],
        "type": "object"
      },
      "RequestKind": {
        "description": "What a data subject is exercising (GDPR Chapter III). Mirrors the `gdpr_request_kind` SQL\nenum, whose `sqlx`-typed counterpart stays in `tankovault_db` — this crate must not depend\non the driver.",
        "enum": [
          "access",
          "portability",
          "rectification",
          "erasure",
          "restriction",
          "objection"
        ],
        "type": "string"
      },
      "RequestRow": {
        "description": "A request as the subject sees it, and the shape the operator queue extends.",
        "properties": {
          "detail": {
            "type": [
              "string",
              "null"
            ]
          },
          "due_at": {
            "type": "string"
          },
          "id": {
            "format": "uuid",
            "type": "string"
          },
          "kind": {
            "$ref": "#/components/schemas/RequestKind"
          },
          "requested_at": {
            "type": "string"
          },
          "resolution_note": {
            "description": "How it was resolved, or — for a rejection — why. Art. 12(4) obliges the controller to\ngive reasons for a refusal, so a rejected request without this is incomplete.",
            "type": [
              "string",
              "null"
            ]
          },
          "resolved_at": {
            "type": [
              "string",
              "null"
            ]
          },
          "status": {
            "$ref": "#/components/schemas/RequestStatus"
          }
        },
        "required": [
          "id",
          "kind",
          "status",
          "requested_at",
          "due_at"
        ],
        "type": "object"
      },
      "RequestStatus": {
        "description": "Where a data-subject request is in its lifecycle. Mirrors the `gdpr_request_status` SQL\nenum; see [`PrivacyRequestKind`] for why the mirror exists.",
        "enum": [
          "pending",
          "in_progress",
          "completed",
          "rejected",
          "cancelled"
        ],
        "type": "string"
      },
      "ResendVerificationRequest": {
        "properties": {
          "email": {
            "description": "The account's email address. Unknown or already-confirmed addresses are accepted\nsilently so the endpoint can't be used to probe which emails have accounts.",
            "type": "string"
          }
        },
        "required": [
          "email"
        ],
        "type": "object"
      },
      "ResetPasswordRequest": {
        "properties": {
          "new_password": {
            "description": "The new password (same policy as registration: at least 8 characters).",
            "type": "string"
          },
          "token": {
            "description": "The opaque token from the emailed reset link.",
            "type": "string"
          }
        },
        "required": [
          "token",
          "new_password"
        ],
        "type": "object"
      },
      "ResolveConflict": {
        "properties": {
          "resolution": {
            "type": "string"
          }
        },
        "required": [
          "resolution"
        ],
        "type": "object"
      },
      "ResolveRequest": {
        "properties": {
          "note": {
            "description": "What was done, or — for a rejection — why.\n\nMandatory on a rejection: Art. 12(4) obliges the controller to give reasons for refusing\nto act, so a rejection without one is not a lawful response.",
            "type": [
              "string",
              "null"
            ]
          },
          "status": {
            "$ref": "#/components/schemas/RequestStatus",
            "description": "How it ends. Only `completed` and `rejected` are operator decisions; `cancelled` belongs\nto the subject and is refused here."
          }
        },
        "required": [
          "status"
        ],
        "type": "object"
      },
      "RunState": {
        "description": "Lifecycle of a scan run.",
        "enum": [
          "queued",
          "running",
          "completed",
          "failed",
          "cancelled"
        ],
        "type": "string"
      },
      "ScanMode": {
        "description": "Scan cadence.",
        "enum": [
          "full",
          "fast"
        ],
        "type": "string"
      },
      "ScanRun": {
        "description": "A scan run (progress + audit; mirrors `JetStream` dispatch).",
        "properties": {
          "created_at": {
            "type": "string"
          },
          "done_tasks": {
            "format": "int32",
            "type": "integer"
          },
          "failed_tasks": {
            "format": "int32",
            "type": "integer"
          },
          "finished_at": {
            "type": [
              "string",
              "null"
            ]
          },
          "id": {
            "$ref": "#/components/schemas/ScanRunId"
          },
          "mode": {
            "$ref": "#/components/schemas/ScanMode"
          },
          "provider_id": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/ProviderId"
              }
            ]
          },
          "started_at": {
            "type": [
              "string",
              "null"
            ]
          },
          "state": {
            "$ref": "#/components/schemas/RunState"
          },
          "total_tasks": {
            "format": "int32",
            "type": "integer"
          }
        },
        "required": [
          "id",
          "mode",
          "state",
          "total_tasks",
          "done_tasks",
          "failed_tasks",
          "created_at"
        ],
        "type": "object"
      },
      "ScanRunId": {
        "description": "Identifies a scan run.",
        "format": "uuid",
        "type": "string"
      },
      "ScanTaskId": {
        "description": "Identifies a scan task.",
        "format": "uuid",
        "type": "string"
      },
      "ScanTriggered": {
        "description": "What the control plane answers when a scan is planned: the runs it created.\n\nPublished by `services/control-plane` on its internal `POST /internal/scans`, and\nrepublished verbatim by `services/api` on `POST /v1/admin/scans` and\n`POST /v1/admin/providers/{id}/resolve`. It lives here, rather than staying a private\nstruct in the control plane's `main.rs`, for the reason ARCH-10 exists: while the producer\nowned the only definition, the API could declare nothing more specific than\n`serde_json::Value`, so the console's \"N scans queued\" was reading a field no compiler had\never connected to the field the planner writes. Both ends now name this type, so removing\n`run_ids` fails to build at the producer *and* the republisher.",
        "properties": {
          "run_ids": {
            "description": "One id per run planned — one per provider when the request names none.",
            "items": {
              "$ref": "#/components/schemas/ScanRunId"
            },
            "type": "array"
          }
        },
        "required": [
          "run_ids"
        ],
        "type": "object"
      },
      "SeriesDetail": {
        "properties": {
          "alt_titles": {
            "description": "Alternative titles gathered across providers (frontend §9.2; empty when none).",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "anilist_id": {
            "description": "`AniList` media id, if this series is mapped (`sync_mappings`); lets the frontend\nlink out to the canonical `AniList` entry regardless of whether the viewer has synced.",
            "type": [
              "string",
              "null"
            ]
          },
          "authors": {
            "description": "Author/artist credits attached to the series; empty when none.",
            "items": {
              "$ref": "#/components/schemas/Author"
            },
            "type": "array"
          },
          "content_type": {
            "$ref": "#/components/schemas/ContentType"
          },
          "cover_url": {
            "type": [
              "string",
              "null"
            ]
          },
          "description": {
            "type": [
              "string",
              "null"
            ]
          },
          "id": {
            "$ref": "#/components/schemas/SeriesId"
          },
          "release_year": {
            "format": "int32",
            "type": [
              "integer",
              "null"
            ]
          },
          "sources": {
            "items": {
              "$ref": "#/components/schemas/SourceDto"
            },
            "type": "array"
          },
          "status": {
            "$ref": "#/components/schemas/SeriesStatus"
          },
          "tags": {
            "description": "Genre/tags attached to the series (frontend §9.2; empty when none).",
            "items": {
              "$ref": "#/components/schemas/Tag"
            },
            "type": "array"
          },
          "title": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "title",
          "content_type",
          "status",
          "sources",
          "alt_titles",
          "tags",
          "authors"
        ],
        "type": "object"
      },
      "SeriesId": {
        "description": "Identifies a canonical [`crate::entities::Series`].",
        "format": "uuid",
        "type": "string"
      },
      "SeriesSourceId": {
        "description": "Identifies a [`crate::entities::SeriesSource`].",
        "format": "uuid",
        "type": "string"
      },
      "SeriesStatus": {
        "description": "Publication status of a canonical series.",
        "enum": [
          "ongoing",
          "completed",
          "hiatus",
          "cancelled",
          "unknown"
        ],
        "type": "string"
      },
      "SeriesSummary": {
        "properties": {
          "content_type": {
            "$ref": "#/components/schemas/ContentType"
          },
          "cover_url": {
            "type": [
              "string",
              "null"
            ]
          },
          "id": {
            "$ref": "#/components/schemas/SeriesId"
          },
          "source_count": {
            "format": "int64",
            "type": "integer"
          },
          "status": {
            "$ref": "#/components/schemas/SeriesStatus"
          },
          "title": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "title",
          "content_type",
          "status",
          "source_count"
        ],
        "type": "object"
      },
      "SessionDto": {
        "properties": {
          "created_at": {
            "type": "string"
          },
          "expires_at": {
            "type": "string"
          },
          "family_id": {
            "type": "string"
          },
          "id": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "family_id",
          "created_at",
          "expires_at"
        ],
        "type": "object"
      },
      "SetFlag": {
        "properties": {
          "enabled": {
            "type": "boolean"
          },
          "note": {
            "description": "Why. Optional, but it is the thing the next operator needs and the audit record does not\nsurface on this page.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "enabled"
        ],
        "type": "object"
      },
      "SetPermissions": {
        "properties": {
          "permissions": {
            "description": "The complete permission set the account should have afterwards.\n\nA whole set rather than add/remove operations: the editor is a checklist, and submitting\nthe intended end state means two administrators editing concurrently produce one of their\ntwo intents rather than an interleaving of both. Unknown tokens are rejected by\ndeserialisation — a capability that does not exist cannot be granted.",
            "items": {
              "$ref": "#/components/schemas/Permission"
            },
            "type": "array"
          }
        },
        "required": [
          "permissions"
        ],
        "type": "object"
      },
      "SetProviderState": {
        "properties": {
          "state": {
            "$ref": "#/components/schemas/ProviderState"
          }
        },
        "required": [
          "state"
        ],
        "type": "object"
      },
      "SetUserStatus": {
        "properties": {
          "reason": {
            "description": "Why the account was suspended. Recorded on the account and shown to whoever looks at it\nnext; ignored when reinstating.",
            "type": [
              "string",
              "null"
            ]
          },
          "revoke_sessions": {
            "description": "Also revoke every live session, so the suspension takes effect immediately rather than\nwhen the current access token expires.\n\nDefaults to `true`: a suspension that leaves the account working for the next quarter of\nan hour is not what anyone means by suspending it. Settable to `false` for the case\nwhere an operator wants the account frozen but the user's current tab left alone.",
            "type": "boolean"
          },
          "status": {
            "$ref": "#/components/schemas/AccountStatus"
          }
        },
        "required": [
          "status"
        ],
        "type": "object"
      },
      "SimilarSeries": {
        "allOf": [
          {
            "$ref": "#/components/schemas/SeriesSummary"
          },
          {
            "properties": {
              "score": {
                "description": "Cosine similarity in `[0, 1]`, as the index ranked it.",
                "format": "float",
                "type": "number"
              },
              "shared": {
                "description": "The features this shares with the seed, most explanatory first — the tag names a reader\nwould recognise, not feature ids.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            },
            "required": [
              "score",
              "shared"
            ],
            "type": "object"
          }
        ],
        "description": "One similar series, with what it has in common."
      },
      "SourceDto": {
        "properties": {
          "chapter_count": {
            "description": "Count of distinct **whole** chapters (`floor(number)`-deduped) — sub-chapter part\nreleases don't inflate this (§ chapter grouping).",
            "format": "int32",
            "type": "integer"
          },
          "id": {
            "$ref": "#/components/schemas/SeriesSourceId"
          },
          "is_primary": {
            "description": "True for the richest source (most chapters) — the one the reader should prefer\n(frontend §9.2). Exactly one source per series is flagged.",
            "type": "boolean"
          },
          "provider_name": {
            "type": "string"
          },
          "provider_slug": {
            "type": "string"
          },
          "url": {
            "description": "Resolved absolute URL to open the series on the provider.",
            "type": "string"
          }
        },
        "required": [
          "id",
          "provider_name",
          "provider_slug",
          "url",
          "chapter_count",
          "is_primary"
        ],
        "type": "object"
      },
      "StreamTicket": {
        "description": "A freshly minted stream ticket.",
        "properties": {
          "expires_in": {
            "description": "Seconds until it expires. Redeem immediately; this is for diagnostics, not scheduling.",
            "format": "int64",
            "minimum": 0,
            "type": "integer"
          },
          "ticket": {
            "description": "The opaque value to pass as `?ticket=` when opening the stream.",
            "type": "string"
          }
        },
        "required": [
          "ticket",
          "expires_in"
        ],
        "type": "object"
      },
      "SuggestedMatch": {
        "description": "One ranked suggestion for the admin \"match every loaded entry\" screen: a local series the\nmatcher thinks the remote entry could be, with enough info (title, type, sources) to\neyeball it and its confidence `score` in `[0,1]`.",
        "properties": {
          "content_type": {
            "type": "string"
          },
          "release_year": {
            "format": "int32",
            "type": [
              "integer",
              "null"
            ]
          },
          "score": {
            "format": "float",
            "type": "number"
          },
          "series_id": {
            "format": "uuid",
            "type": "string"
          },
          "source_count": {
            "format": "int64",
            "type": "integer"
          },
          "title": {
            "type": "string"
          }
        },
        "required": [
          "series_id",
          "title",
          "content_type",
          "source_count",
          "score"
        ],
        "type": "object"
      },
      "SyncAccountStatus": {
        "description": "Whether a user has linked a provider and, when linked, who they are connected as.\n\nAlways `200`: an unlinked account reads `{ \"linked\": false }` rather than 404ing, so the\nUI can render \"not connected\" without treating it as an error.\n\nPublished as `SyncAccountStatus` rather than under its Rust name: `tankovault_domain`\nhas an `AccountStatus` too (whether a *user account* is active or suspended), and two\nunrelated types sharing one `OpenAPI` component name means the last one registered silently\nreplaces the other — which is exactly what happened when the domain enum was introduced.\nThe qualifier is on this one because it is the more specific of the two: an\nexternal-tracker link status, not an account's own state.",
        "properties": {
          "last_synced_at": {
            "description": "RFC-3339 timestamp of the most recent successful sync, when there has been one.",
            "type": [
              "string",
              "null"
            ]
          },
          "linked": {
            "description": "Whether a linked external account exists for this user and provider.",
            "type": "boolean"
          },
          "username": {
            "description": "The connected account's display name at the provider, when linked.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "linked"
        ],
        "type": "object"
      },
      "SyncAccountTarget": {
        "properties": {
          "provider": {
            "type": "string"
          },
          "user_id": {
            "$ref": "#/components/schemas/UserId"
          }
        },
        "required": [
          "user_id",
          "provider"
        ],
        "type": "object"
      },
      "SyncExcluded": {
        "properties": {
          "excluded": {
            "type": "boolean"
          }
        },
        "required": [
          "excluded"
        ],
        "type": "object"
      },
      "SyncMappingTarget": {
        "properties": {
          "provider": {
            "type": "string"
          },
          "series_id": {
            "$ref": "#/components/schemas/SeriesId"
          }
        },
        "required": [
          "series_id",
          "provider"
        ],
        "type": "object"
      },
      "SyncOpts": {
        "properties": {
          "policy": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/ConflictPolicy",
                "description": "Overrides the account's own conflict policy for this run; omitted uses it.\n\nTyped rather than a bare string since FRONTEND F10: the sync service's request body\nhas always been a `ConflictPolicy`, so a token this proxy accepted and that one did not\nused to fail one hop downstream, as a `502` with no useful detail. Now the two name the\nsame type and the rejection happens here, as a `422` that says which values are legal."
              }
            ]
          }
        },
        "type": "object"
      },
      "SyncSettingsPatch": {
        "properties": {
          "auto_sync_enabled": {
            "type": [
              "boolean",
              "null"
            ]
          },
          "conflict_policy": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/ConflictPolicy",
                "description": "The account's new conflict policy; omitted leaves it unchanged.\n\nSee [`SyncOpts::policy`] for why this is typed rather than a bare string."
              }
            ]
          }
        },
        "type": "object"
      },
      "SystemStats": {
        "description": "System-wide rollup for the console header.",
        "properties": {
          "chapters_1h": {
            "format": "int64",
            "type": "integer"
          },
          "chapters_24h": {
            "format": "int64",
            "type": "integer"
          },
          "chapters_7d": {
            "format": "int64",
            "type": "integer"
          },
          "chapters_total": {
            "format": "int64",
            "type": "integer"
          },
          "pending_merges": {
            "format": "int64",
            "type": "integer"
          },
          "providers_active": {
            "format": "int64",
            "type": "integer"
          },
          "providers_disabled": {
            "format": "int64",
            "type": "integer"
          },
          "providers_total": {
            "format": "int64",
            "type": "integer"
          },
          "providers_unhealthy": {
            "description": "Providers in a non-serving health state (degraded/challenged/solving/blocked).",
            "format": "int64",
            "type": "integer"
          },
          "runs_active": {
            "description": "Scan runs currently queued or running.",
            "format": "int64",
            "type": "integer"
          },
          "runs_running": {
            "format": "int64",
            "type": "integer"
          },
          "series_total": {
            "format": "int64",
            "type": "integer"
          },
          "sources_total": {
            "format": "int64",
            "type": "integer"
          },
          "tasks_failed_24h": {
            "format": "int64",
            "type": "integer"
          },
          "tasks_queued": {
            "format": "int64",
            "type": "integer"
          },
          "tasks_running": {
            "format": "int64",
            "type": "integer"
          },
          "users_total": {
            "format": "int64",
            "type": "integer"
          }
        },
        "required": [
          "providers_total",
          "providers_active",
          "providers_disabled",
          "providers_unhealthy",
          "series_total",
          "sources_total",
          "chapters_total",
          "chapters_1h",
          "chapters_24h",
          "chapters_7d",
          "users_total",
          "pending_merges",
          "runs_active",
          "runs_running",
          "tasks_queued",
          "tasks_running",
          "tasks_failed_24h"
        ],
        "type": "object"
      },
      "Tag": {
        "description": "A genre/tag.",
        "properties": {
          "id": {
            "$ref": "#/components/schemas/TagId"
          },
          "name": {
            "type": "string"
          },
          "slug": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "slug",
          "name"
        ],
        "type": "object"
      },
      "TagId": {
        "description": "Identifies a tag.",
        "format": "uuid",
        "type": "string"
      },
      "TaskState": {
        "description": "Lifecycle of an individual scan task.",
        "enum": [
          "queued",
          "claimed",
          "running",
          "done",
          "failed",
          "skipped"
        ],
        "type": "string"
      },
      "TasteFeature": {
        "description": "One weighted feature in a taste profile.",
        "properties": {
          "kind": {
            "type": "string"
          },
          "value": {
            "type": "string"
          },
          "weight": {
            "format": "float",
            "type": "number"
          }
        },
        "required": [
          "kind",
          "value",
          "weight"
        ],
        "type": "object"
      },
      "TasteView": {
        "description": "The reader's own taste profile, rendered.",
        "properties": {
          "avoids": {
            "description": "Features the reader has rejected, strongest first.",
            "items": {
              "$ref": "#/components/schemas/TasteFeature"
            },
            "type": "array"
          },
          "built_at": {
            "description": "RFC 3339. A `String` rather than an `OffsetDateTime` because `utoipa` has no schema for\nthe latter, and the published contract should name the format either way.",
            "example": "2026-08-04T12:00:00Z",
            "type": "string"
          },
          "likes": {
            "description": "Features the reader gravitates to, strongest first.",
            "items": {
              "$ref": "#/components/schemas/TasteFeature"
            },
            "type": "array"
          },
          "seeds": {
            "description": "Series the profile was built from, in affinity order.",
            "items": {
              "$ref": "#/components/schemas/SeriesId"
            },
            "type": "array"
          }
        },
        "required": [
          "likes",
          "avoids",
          "seeds",
          "built_at"
        ],
        "type": "object"
      },
      "TestAdapterRequest": {
        "properties": {
          "path": {
            "description": "Optional relative series path to also fetch metadata + chapters for.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "type": "object"
      },
      "TokenResponse": {
        "properties": {
          "access_token": {
            "type": "string"
          },
          "expires_in": {
            "format": "int64",
            "type": "integer"
          },
          "token_type": {
            "type": "string"
          }
        },
        "required": [
          "access_token",
          "token_type",
          "expires_in"
        ],
        "type": "object"
      },
      "TriggerScan": {
        "properties": {
          "mode": {
            "$ref": "#/components/schemas/ScanMode"
          },
          "provider_id": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/ProviderId"
              }
            ]
          }
        },
        "required": [
          "mode"
        ],
        "type": "object"
      },
      "UnmappedSeriesRow": {
        "description": "One row of the admin Sync console's \"Assign queue\" — a canonical series that has **no**\nexternal mapping for the given provider yet, so an operator can review and assign one.",
        "properties": {
          "series_id": {
            "format": "uuid",
            "type": "string"
          },
          "series_title": {
            "type": "string"
          },
          "source_count": {
            "description": "How many local sources back this series (a proxy for how confident a match is worth).",
            "format": "int64",
            "type": "integer"
          }
        },
        "required": [
          "series_id",
          "series_title",
          "source_count"
        ],
        "type": "object"
      },
      "UpdateProvider": {
        "properties": {
          "base_url": {
            "type": "string"
          },
          "config": {},
          "name": {
            "type": "string"
          },
          "politeness": {
            "$ref": "#/components/schemas/Politeness"
          }
        },
        "required": [
          "name",
          "base_url"
        ],
        "type": "object"
      },
      "UpsertMapping": {
        "properties": {
          "external_id": {
            "type": "string"
          },
          "provider": {
            "type": "string"
          },
          "series_id": {
            "$ref": "#/components/schemas/SeriesId"
          }
        },
        "required": [
          "series_id",
          "provider",
          "external_id"
        ],
        "type": "object"
      },
      "UserDetail": {
        "description": "Everything the user-detail panel shows, minus the grant list (fetched separately by\n`tankovault_db::repo::permissions::list_for_user` so the panel can refresh just that part\nafter an edit).",
        "properties": {
          "active_sessions": {
            "description": "Live login sessions. Tells an operator whether a suspension will actually take effect\nwithout also revoking sessions.",
            "format": "int64",
            "type": "integer"
          },
          "created_at": {
            "type": "string"
          },
          "email": {
            "type": "string"
          },
          "email_verified": {
            "type": "boolean"
          },
          "id": {
            "type": "string"
          },
          "last_login_at": {
            "type": [
              "string",
              "null"
            ]
          },
          "linked_accounts": {
            "description": "Linked external trackers, so an operator can see the account has third-party\ncredentials at rest before deciding to erase it.",
            "format": "int64",
            "type": "integer"
          },
          "open_privacy_requests": {
            "description": "Unresolved data-subject requests filed by this user. An account with an open erasure\nrequest must not be quietly edited.",
            "format": "int64",
            "type": "integer"
          },
          "status": {
            "$ref": "#/components/schemas/AccountStatus"
          },
          "suspended_at": {
            "type": [
              "string",
              "null"
            ]
          },
          "suspension_reason": {
            "type": [
              "string",
              "null"
            ]
          },
          "tracked_count": {
            "format": "int64",
            "type": "integer"
          },
          "username": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "email",
          "username",
          "status",
          "email_verified",
          "created_at",
          "active_sessions",
          "tracked_count",
          "linked_accounts",
          "open_privacy_requests"
        ],
        "type": "object"
      },
      "UserDetailResponse": {
        "description": "One account's administrative detail, together with its grants.",
        "properties": {
          "permissions": {
            "description": "The account's permission grants, with provenance.",
            "items": {
              "$ref": "#/components/schemas/GrantRow"
            },
            "type": "array"
          },
          "user": {
            "$ref": "#/components/schemas/UserDetail"
          }
        },
        "required": [
          "user",
          "permissions"
        ],
        "type": "object"
      },
      "UserId": {
        "description": "Identifies a user account.",
        "format": "uuid",
        "type": "string"
      },
      "Value": {},
      "VerifyEmailRequest": {
        "properties": {
          "token": {
            "description": "The opaque token from the emailed confirmation link.",
            "type": "string"
          }
        },
        "required": [
          "token"
        ],
        "type": "object"
      },
      "WatchStatus": {
        "description": "A user's tracking status for a series.",
        "enum": [
          "reading",
          "planned",
          "completed",
          "dropped",
          "paused"
        ],
        "type": "string"
      },
      "WatchlistBulkIds": {
        "description": "The ids a bulk removal or bulk mark-read should act on.",
        "properties": {
          "series_ids": {
            "items": {
              "$ref": "#/components/schemas/SeriesId"
            },
            "type": "array"
          }
        },
        "required": [
          "series_ids"
        ],
        "type": "object"
      },
      "WatchlistBulkUpdate": {
        "description": "The ids a bulk operation should act on, plus what to change.",
        "properties": {
          "notify": {
            "description": "Leave absent to keep each entry's current notification setting.",
            "type": [
              "boolean",
              "null"
            ]
          },
          "series_ids": {
            "items": {
              "$ref": "#/components/schemas/SeriesId"
            },
            "type": "array"
          },
          "status": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/WatchStatus",
                "description": "Leave absent to keep each entry's current status."
              }
            ]
          }
        },
        "required": [
          "series_ids"
        ],
        "type": "object"
      },
      "WatchlistCounts": {
        "description": "Entry counts per status, for the tab strip.",
        "properties": {
          "all": {
            "format": "int64",
            "type": "integer"
          },
          "completed": {
            "format": "int64",
            "type": "integer"
          },
          "dropped": {
            "format": "int64",
            "type": "integer"
          },
          "paused": {
            "format": "int64",
            "type": "integer"
          },
          "planned": {
            "format": "int64",
            "type": "integer"
          },
          "reading": {
            "format": "int64",
            "type": "integer"
          },
          "source_issues": {
            "description": "Rows whose preferred source is unhealthy, for the toolbar's `Source issues` chip.",
            "format": "int64",
            "type": "integer"
          }
        },
        "required": [
          "reading",
          "planned",
          "paused",
          "completed",
          "dropped",
          "all",
          "source_issues"
        ],
        "type": "object"
      },
      "WatchlistEntryView": {
        "description": "One series' watchlist entry, or `null` when the caller does not track it.\n\nAn object wrapping a nullable field rather than a bare nullable body: \"not tracked\" is a\nperfectly ordinary answer to this question, so it is a `200` with `entry: null` rather than\na `404` the client has to special-case out of its error path.",
        "properties": {
          "entry": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/WatchlistItem"
              }
            ]
          }
        },
        "type": "object"
      },
      "WatchlistGroup": {
        "description": "One release-recency band's aggregates, for a group header.",
        "properties": {
          "chapter_count": {
            "format": "int64",
            "type": "integer"
          },
          "key": {
            "description": "`today` | `week` | `earlier`.",
            "type": "string"
          },
          "title_count": {
            "format": "int64",
            "type": "integer"
          }
        },
        "required": [
          "key",
          "title_count",
          "chapter_count"
        ],
        "type": "object"
      },
      "WatchlistItem": {
        "properties": {
          "added_at": {
            "type": "string"
          },
          "cover_url": {
            "type": [
              "string",
              "null"
            ]
          },
          "last_read_number": {
            "description": "The user's last-read chapter number, if any.",
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "latest_chapter_at": {
            "description": "When the newest chapter was discovered. Drives the default sort, the `Released` column\nand the Today/This week/Earlier grouping.",
            "type": [
              "string",
              "null"
            ]
          },
          "latest_chapter_number": {
            "description": "The highest chapter number known across all sources.",
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "next_unread": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/NextUnread",
                "description": "The lowest unread chapter, or `null` when the reader is caught up."
              }
            ]
          },
          "notify": {
            "type": "boolean"
          },
          "preferred_source_name": {
            "description": "Display name of the provider this series is primarily carried by.",
            "type": [
              "string",
              "null"
            ]
          },
          "read_count": {
            "description": "Whole chapters at or below the user's progress — the progress bar's numerator.\n\nNot [`Self::last_read_number`]: a catalogue with gaps makes the frontier larger than the\nnumber of chapters below it, so a bar drawn from the frontier reads past 100%.",
            "format": "int64",
            "type": "integer"
          },
          "series_id": {
            "$ref": "#/components/schemas/SeriesId"
          },
          "series_title": {
            "description": "Embedded series title so the Watchlist renders without a per-row detail fetch\n(frontend §9.3, kills the N+1).",
            "type": "string"
          },
          "source_count": {
            "description": "Distinct providers carrying this series.",
            "format": "int64",
            "type": "integer"
          },
          "source_degraded": {
            "description": "Whether the preferred source, or the provider behind it, is unhealthy.",
            "type": "boolean"
          },
          "sources": {
            "description": "Every provider carrying this series, preferred first.",
            "items": {
              "$ref": "#/components/schemas/WatchlistSource"
            },
            "type": "array"
          },
          "status": {
            "$ref": "#/components/schemas/WatchStatus"
          },
          "sync_excluded": {
            "description": "Whether this series is opted out of external sync (design v2 §A.5).",
            "type": "boolean"
          },
          "total_chapters": {
            "description": "Distinct whole chapters known across all sources — the progress bar's denominator.",
            "format": "int64",
            "type": "integer"
          },
          "unread": {
            "description": "Unread chapters above the user's progress.",
            "format": "int64",
            "type": "integer"
          }
        },
        "required": [
          "series_id",
          "status",
          "notify",
          "added_at",
          "series_title",
          "unread",
          "read_count",
          "sources",
          "total_chapters",
          "source_count",
          "source_degraded",
          "sync_excluded"
        ],
        "type": "object"
      },
      "WatchlistSource": {
        "description": "One provider carrying a series, for the ledger's `Sources` column.",
        "properties": {
          "code": {
            "description": "The provider slug — the monogram tile's letters and a stable client-side key.",
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "preferred": {
            "description": "Whether this is the source `preferred_source_name` names.",
            "type": "boolean"
          },
          "state": {
            "$ref": "#/components/schemas/ProviderState",
            "description": "The effective state: the series-source's own, unless the provider behind it is worse."
          }
        },
        "required": [
          "code",
          "name",
          "state",
          "preferred"
        ],
        "type": "object"
      },
      "WatchlistSummaryView": {
        "description": "The whole watchlist at a glance, under no filters at all.",
        "properties": {
          "counts": {
            "$ref": "#/components/schemas/WatchlistCounts"
          },
          "unread_total": {
            "description": "Unread chapters across every tracked series, whatever its status.",
            "format": "int64",
            "type": "integer"
          }
        },
        "required": [
          "counts",
          "unread_total"
        ],
        "type": "object"
      },
      "WatchlistUpsert": {
        "properties": {
          "notify": {
            "type": "boolean"
          },
          "status": {
            "$ref": "#/components/schemas/WatchStatus"
          }
        },
        "type": "object"
      },
      "WatchlistView": {
        "description": "The watchlist page plus the chrome around it.",
        "properties": {
          "counts": {
            "$ref": "#/components/schemas/WatchlistCounts"
          },
          "groups": {
            "description": "Newest band first; empty bands are omitted.",
            "items": {
              "$ref": "#/components/schemas/WatchlistGroup"
            },
            "type": "array"
          },
          "items": {
            "items": {
              "$ref": "#/components/schemas/WatchlistItem"
            },
            "type": "array"
          },
          "next_cursor": {
            "description": "Pass back as `cursor` to fetch the next page; `null` at the end of the list.\n\nOpaque on purpose: it encodes the sort key the server ordered by, which changes shape\nwith `sort`, and a client that parsed it would break the first time an order was added.",
            "type": [
              "string",
              "null"
            ]
          },
          "total": {
            "description": "Rows matching the whole filter, `status` included — the pager's denominator.",
            "format": "int64",
            "type": "integer"
          }
        },
        "required": [
          "items",
          "total",
          "counts",
          "groups"
        ],
        "type": "object"
      }
    },
    "securitySchemes": {
      "bearer_auth": {
        "scheme": "bearer",
        "type": "http"
      },
      "stream_ticket": {
        "description": "Single-use, 30-second ticket from `POST /v1/me/stream-ticket`. `EventSource` cannot set an `Authorization` header, so the credential has to ride in the URL; making it single-use and short-lived is what stops the access log, `Referer` header and browser history that record it from being worth reading (SEC-8).",
        "in": "query",
        "name": "ticket",
        "type": "apiKey"
      }
    }
  },
  "info": {
    "contact": {
      "name": "TankoVault contributors"
    },
    "description": "Axum HTTP edge: public read/write + admin, auth, resolved links, SSE.",
    "license": {
      "name": ""
    },
    "title": "tankovault-api",
    "version": "1.2.0"
  },
  "openapi": "3.1.0",
  "paths": {
    "/v1/admin/audit": {
      "get": {
        "description": "The most recent privileged actions (design §16 audit trail).",
        "operationId": "audit_log",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/AuditView"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Up to 40 most recent audit records"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "the audit trail feature is disabled"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Get the audit log",
        "tags": [
          "admin-overview"
        ]
      }
    },
    "/v1/admin/feature-flags": {
      "get": {
        "description": "Every feature this build defines, with its effective state, its shipped default, and who\nlast changed it. Served from the compiled registry joined to the stored overrides, so the\npage can never list a feature that does nothing or omit one that does.",
        "operationId": "list_flags",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/FlagView"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Every feature and its current state"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "List feature flags",
        "tags": [
          "admin-feature-flags"
        ]
      }
    },
    "/v1/admin/feature-flags/{key}": {
      "delete": {
        "description": "Drops the stored override so the feature follows the value it ships with. Distinct from\nsetting it to that value, which records an operator decision that would survive a future\nchange of the default.",
        "operationId": "reset_flag",
        "parameters": [
          {
            "description": "Feature key, e.g. `sync.auto_push`",
            "in": "path",
            "name": "key",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/FlagView"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Every feature and its state after the reset"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "unknown feature"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Reset a feature flag",
        "tags": [
          "admin-feature-flags"
        ]
      },
      "put": {
        "description": "Records an explicit decision for one feature and applies it immediately on this replica.\nWriting the value a feature already has is meaningful: it pins the feature against a future\nchange of the shipped default.",
        "operationId": "set_flag",
        "parameters": [
          {
            "description": "Feature key, e.g. `sync.auto_push`",
            "in": "path",
            "name": "key",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetFlag"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/FlagView"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Every feature and its state after the change"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "unknown feature, or an attempt to disable a locked one"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Set a feature flag",
        "tags": [
          "admin-feature-flags"
        ]
      }
    },
    "/v1/admin/matching/rebuild-keys": {
      "post": {
        "description": "Re-derives `series.normalized_title` and `series_titles.normalized` for the whole catalogue\nthrough the current `normalize_title`.\n\n# Why this is an operator action\n\nThe normalized title is a *persisted* key: it is written once, when a series is created, and\nevery later match compares against the stored value. A change to the normalization rules —\nlike making an apostrophe join a word instead of splitting one — therefore leaves the entire\ncatalogue on keys derived by the previous rules, and the improvement only reaches rows that\nhappen to be re-scanned. Running this is what makes a rules change take effect, and it is\nsafe to run repeatedly: only rows whose key actually changed are written.",
        "operationId": "rebuild_matching_keys",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/KeyRebuildView"
                }
              }
            },
            "description": "What the rebuild changed"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Rebuild the normalized matching keys",
        "tags": [
          "admin-matching"
        ]
      }
    },
    "/v1/admin/merge-candidates": {
      "get": {
        "description": "The canonicalisation review queue (design §10), highest confidence first.",
        "operationId": "list_merge_candidates",
        "parameters": [
          {
            "description": "Only return candidates at or above this confidence, in `[0,1]`.\n\nAn operator works a large queue in bands: everything above 0.9 is nearly all genuine\nduplicates and can be actioned quickly, while the 0.6–0.7 band needs real attention per\nrow. Out-of-range values are clamped rather than rejected — this narrows a list, so the\nworst a nonsense value can do is return nothing.",
            "in": "query",
            "name": "min_score",
            "required": false,
            "schema": {
              "format": "float",
              "type": [
                "number",
                "null"
              ]
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/MergeCandidateView"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Up to 200 open merge candidates, highest score first"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "List merge candidates",
        "tags": [
          "admin-matching"
        ]
      }
    },
    "/v1/admin/merge-candidates/dismiss": {
      "post": {
        "description": "Operator judged the two works distinct. The judgement is durable: the pair is suppressed\nagainst re-scans and against the standing duplicate sweep, which is what the previous\nimplementation could not promise — a dismissed pair could be re-inserted as a fresh open row\nby the next scan that saw it.",
        "operationId": "dismiss_merge_candidate",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DismissRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "dismissed": true
                },
                "schema": {}
              }
            },
            "description": "Whether a candidate was actually dismissed"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Dismiss a merge candidate",
        "tags": [
          "admin-matching"
        ]
      }
    },
    "/v1/admin/merge-candidates/sweep": {
      "post": {
        "description": "Re-blocks the whole catalogue on the whitespace-insensitive title key, re-scores every\nshortlisted pair with everything now known about both series, merges the certain ones and\nqueues the rest.\n\nForwarded to the control-plane, which owns the schedule and the leader election that keeps\ntwo replicas from racing on the same destructive merge. The scheduled sweep is the normal\npath; this is here so an operator changing a threshold can see the effect on one run.",
        "operationId": "sweep_merge_candidates",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MergeSweepView"
                }
              }
            },
            "description": "What the sweep did"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "automatic duplicate merging is switched off"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Run the duplicate sweep",
        "tags": [
          "admin-matching"
        ]
      }
    },
    "/v1/admin/permissions": {
      "get": {
        "description": "Every capability this build defines, with its grouping and description, plus the preset\nbundles the editor offers. Served from the compiled registry, so the editor can never list a\ncapability the backend does not enforce or miss one it does.",
        "operationId": "permission_catalogue",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PermissionCatalogue"
                }
              }
            },
            "description": "The permission catalogue"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "List assignable permissions",
        "tags": [
          "admin-users"
        ]
      }
    },
    "/v1/admin/privacy/requests": {
      "get": {
        "description": "The privacy queue, most urgent first: open requests ordered by their Art. 12(3) deadline,\neach flagged if that deadline has passed.",
        "operationId": "list_privacy_queue",
        "parameters": [
          {
            "description": "Show only unresolved requests. Defaults to `true` — the queue's job is the work\noutstanding, and resolved requests are the compliance record behind it.",
            "in": "query",
            "name": "include_resolved",
            "required": false,
            "schema": {
              "type": "boolean"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/AdminRequestRow"
                  },
                  "type": "array"
                }
              }
            },
            "description": "The request queue"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "List data-subject requests",
        "tags": [
          "admin-privacy"
        ]
      }
    },
    "/v1/admin/privacy/requests/{id}/claim": {
      "post": {
        "description": "Takes ownership of a pending request so two operators do not work the same one. Refuses if\nsomebody already claimed it, rather than silently taking it over.",
        "operationId": "claim_privacy_request",
        "parameters": [
          {
            "description": "Request id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AdminRequestRow"
                }
              }
            },
            "description": "The claimed request"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "no such request"
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "already claimed or already resolved"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Claim a data-subject request",
        "tags": [
          "admin-privacy"
        ]
      }
    },
    "/v1/admin/privacy/requests/{id}/export": {
      "get": {
        "description": "Produces the same document as the subject's own export, for delivery in answer to an access\n(Art. 15) or portability (Art. 20) request.\n\nGated on [`Permission::PrivacyExport`] rather than `privacy.write`, and audited under its own\naction: this is the one operation in the queue that *discloses* another person's entire\nrecord, and it needs to be separately grantable and separately searchable afterwards.\n\nRefused for request kinds that are not about disclosure — an erasure request is not a licence\nto read the account first.",
        "operationId": "export_subject_data",
        "parameters": [
          {
            "description": "Request id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "The subject's complete personal-data export"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "this request kind does not call for an export"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "no such request, or its subject no longer exists"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Export a subject's data for a request",
        "tags": [
          "admin-privacy"
        ]
      }
    },
    "/v1/admin/privacy/requests/{id}/extend": {
      "post": {
        "description": "Art. 12(3) allows up to two further months for complex requests. Only moves a deadline\nlater: the subject has been told a date, and a controller shortening its own window after\nthe fact is not an extension.",
        "operationId": "extend_privacy_request",
        "parameters": [
          {
            "description": "Request id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ExtendRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AdminRequestRow"
                }
              }
            },
            "description": "The request with its new deadline"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "the request is resolved, or the new date is not later"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "no such request"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Extend a request's deadline",
        "tags": [
          "admin-privacy"
        ]
      }
    },
    "/v1/admin/privacy/requests/{id}/fulfil-erasure": {
      "post": {
        "description": "Erases the subject's account and everything it owns, then resolves the request as completed\nin the same call — so the compliance record cannot end up saying an erasure was done when it\nwas not, or the reverse.\n\nRequires [`Permission::UsersDelete`] as well as [`Permission::PrivacyWrite`]: working the\nprivacy queue and being able to destroy an account are separate authorities, and this is the\none action that needs both.",
        "operationId": "fulfil_erasure",
        "parameters": [
          {
            "description": "Request id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/FulfilErasure"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AdminRequestRow"
                }
              }
            },
            "description": "The completed request"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "not an erasure request, or the confirmation did not match"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permissions"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "no such request, or its subject no longer exists"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Carry out an erasure request",
        "tags": [
          "admin-privacy"
        ]
      }
    },
    "/v1/admin/privacy/requests/{id}/resolve": {
      "post": {
        "description": "Marks a request completed or rejected. Only transitions out of an open state, so a\nresolution cannot be recorded twice or rewritten later.\n\nDoes **not** perform the underlying action — completing an erasure request does not erase.\nFulfilment is [`fulfil_erasure`] and [`export_subject_data`], each with its own permission,\nso \"we said we did it\" and \"we did it\" are separate records.",
        "operationId": "resolve_privacy_request",
        "parameters": [
          {
            "description": "Request id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ResolveRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AdminRequestRow"
                }
              }
            },
            "description": "The resolved request"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "invalid target status, or a rejection with no reason"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "no such request"
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "the request was already resolved"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Resolve a data-subject request",
        "tags": [
          "admin-privacy"
        ]
      }
    },
    "/v1/admin/providers": {
      "get": {
        "operationId": "list_providers",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/Provider"
                  },
                  "type": "array"
                }
              }
            },
            "description": "All providers"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "List providers",
        "tags": [
          "admin-providers"
        ]
      },
      "post": {
        "operationId": "create_provider",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateProvider"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Provider"
                }
              }
            },
            "description": "Created"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Provider slug is not a legal bus token"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Provider slug already exists"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Create a provider",
        "tags": [
          "admin-providers"
        ]
      }
    },
    "/v1/admin/providers/stats": {
      "get": {
        "description": "Per-provider crawl statistics table.",
        "operationId": "provider_stats",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/ProviderStat"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Per-provider stats"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Get per-provider crawl stats",
        "tags": [
          "admin-providers"
        ]
      }
    },
    "/v1/admin/providers/{id}": {
      "delete": {
        "description": "Remove a provider entirely. Its stored source links cascade-delete (FK `ON DELETE\nCASCADE`); scan-run history is retained with a nulled provider. Admin-only because it is\ndestructive and irreversible.",
        "operationId": "delete_provider",
        "parameters": [
          {
            "description": "Provider id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/ProviderId"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "ok": true
                },
                "schema": {}
              }
            },
            "description": "Acknowledged"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Provider not found"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Delete a provider",
        "tags": [
          "admin-providers"
        ]
      },
      "patch": {
        "description": "Includes the domain-migration `base_url` change: one field, and every stored relative link\nre-resolves against the new domain.",
        "operationId": "update_provider",
        "parameters": [
          {
            "description": "Provider id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/ProviderId"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateProvider"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Provider"
                }
              }
            },
            "description": "Updated"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Provider not found"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Update a provider",
        "tags": [
          "admin-providers"
        ]
      }
    },
    "/v1/admin/providers/{id}/resolve": {
      "post": {
        "description": "Re-solve/refresh a single provider by queuing a **fast** re-scan (frontend §9.5). This is\nthe console \"Re-solve\" action; it is proxied to the control-plane planner exactly like\n[`trigger_scan`], scoped to one provider.",
        "operationId": "resolve_provider",
        "parameters": [
          {
            "description": "Provider id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/ProviderId"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ScanTriggered"
                }
              }
            },
            "description": "Scan queued, forwarded from the control-plane"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Provider not found"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Re-solve a provider",
        "tags": [
          "admin-providers"
        ]
      }
    },
    "/v1/admin/providers/{id}/state": {
      "post": {
        "description": "Operator override of a provider's health state: `disabled` pauses all crawling; `active`\nre-enables it (e.g. clearing a tripped circuit breaker). The scanner/circuit breaker may\nstill transition it afterwards.",
        "operationId": "set_provider_state",
        "parameters": [
          {
            "description": "Provider id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/ProviderId"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetProviderState"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Provider"
                }
              }
            },
            "description": "Updated"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Provider not found"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Set a provider's health state",
        "tags": [
          "admin-providers"
        ]
      }
    },
    "/v1/admin/providers/{id}/test": {
      "post": {
        "description": "Dry-run the provider's adapter against the live site and return the parsed sample so\noperators can fix selectors without a deploy (design §11/§17). Bounded by a timeout;\nSSRF and rate limits are enforced by the injected fetch stack.\n\nThe body is deliberately free-form JSON: it is a diagnostic dump whose shape follows\nwhatever the adapter managed to parse, and the console renders it verbatim. It is still\ndeclared as a schema so the generated client can return it, rather than forcing callers\nonto an untyped side channel.",
        "operationId": "test_adapter",
        "parameters": [
          {
            "description": "Provider id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/ProviderId"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/components/schemas/TestAdapterRequest"
                  }
                ]
              }
            }
          },
          "description": "Optional relative series path to also fetch metadata + chapters for"
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "latest": {
                    "items": [],
                    "ok": true
                  },
                  "provider": "kunmanga"
                },
                "schema": {}
              }
            },
            "description": "Dry-run sample (adapter list/fetch results, each individually ok/error)"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Adapter build failed or the dry-run timed out"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Provider not found"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Dry-run a provider's adapter",
        "tags": [
          "admin-providers"
        ]
      }
    },
    "/v1/admin/scan-failures": {
      "get": {
        "description": "The most recently failed scan tasks with their errors, for triaging stuck providers /\nbroken selectors (design §17.2.7).",
        "operationId": "scan_failures",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/FailedTaskView"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Up to 25 most recent failed tasks"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "List recent scan failures",
        "tags": [
          "admin-scans"
        ]
      }
    },
    "/v1/admin/scans": {
      "get": {
        "description": "The most recent scan runs (the console's scan-queue overview). The live variant is\n`/v1/admin/scans/stream`; this GET gives the console its first paint and drives its\npolling refresh.",
        "operationId": "list_scans",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/ScanRun"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Up to 30 most recent scan runs"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "List recent scan runs",
        "tags": [
          "admin-scans"
        ]
      },
      "post": {
        "description": "Proxied to the control-plane planner.",
        "operationId": "trigger_scan",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TriggerScan"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ScanTriggered"
                }
              }
            },
            "description": "Scan queued, forwarded from the control-plane"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "manual scanning, or full scans specifically, are switched off"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Trigger a scan",
        "tags": [
          "admin-scans"
        ]
      }
    },
    "/v1/admin/scans/stream": {
      "get": {
        "description": "SSE live scan progress for the operator console. Polls the durable `scan_runs` (the system\nof record for progress) every 2 s and pushes a `runs` event; a `scan.progress` NATS relay\nis a documented enhancement.",
        "operationId": "scan_stream",
        "responses": {
          "200": {
            "content": {
              "text/event-stream": {}
            },
            "description": "SSE stream of `runs` events"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Live scan-progress stream",
        "tags": [
          "admin-scans"
        ]
      }
    },
    "/v1/admin/scans/{run_id}": {
      "get": {
        "operationId": "get_scan",
        "parameters": [
          {
            "description": "Scan run id",
            "in": "path",
            "name": "run_id",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/ScanRunId"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ScanRun"
                }
              }
            },
            "description": "Scan run"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Scan run not found"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Get a scan run",
        "tags": [
          "admin-scans"
        ]
      }
    },
    "/v1/admin/series/merge": {
      "post": {
        "description": "Transactional re-parent + title/tag/progress/sync union (design §10).",
        "operationId": "merge_series",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MergeRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "ok": true
                },
                "schema": {}
              }
            },
            "description": "Acknowledged"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "One or both series not found"
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "`keep` and `merge` are the same series"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Merge two series",
        "tags": [
          "admin-matching"
        ]
      }
    },
    "/v1/admin/stats": {
      "get": {
        "description": "System-wide rollup for the console header.",
        "operationId": "system_stats",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SystemStats"
                }
              }
            },
            "description": "System-wide stats"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "the system statistics feature is disabled"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Get system stats",
        "tags": [
          "admin-overview"
        ]
      }
    },
    "/v1/admin/sync/accounts": {
      "get": {
        "description": "Every linked external account across all users.",
        "operationId": "list_sync_accounts",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/AdminAccountRow"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Up to 200 linked accounts"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "List linked external accounts",
        "tags": [
          "admin-sync"
        ]
      }
    },
    "/v1/admin/sync/assign": {
      "post": {
        "description": "Hand-assign a fetched remote entry to a local series. It records the mapping, imports the\nentry onto the user's watchlist (status + progress from the stored snapshot) so the result\nshows immediately, and clears it from the unmatched queue — no fresh pull required.",
        "operationId": "assign_remote_entry",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AssignRemoteEntry"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "ok": true
                },
                "schema": {}
              }
            },
            "description": "Acknowledged"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Missing provider/external_id, no such remote entry, or the stored entry has an invalid status"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Assign a remote entry to a local series",
        "tags": [
          "admin-sync"
        ]
      }
    },
    "/v1/admin/sync/mappings": {
      "get": {
        "description": "Every series↔external mapping across all providers.",
        "operationId": "list_sync_mappings",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/AdminMappingRow"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Up to 200 mappings"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "List series↔external mappings",
        "tags": [
          "admin-sync"
        ]
      },
      "post": {
        "description": "Manually create or correct a series↔external mapping (design: admin Sync console tab).\nLets an operator fix a wrong external id or add a missing one by hand from the per-series\n\"manga info\" editor and the assign queue.",
        "operationId": "upsert_sync_mapping",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpsertMapping"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "ok": true
                },
                "schema": {}
              }
            },
            "description": "Acknowledged"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "provider or external_id is empty"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Create or correct a series↔external mapping",
        "tags": [
          "admin-sync"
        ]
      }
    },
    "/v1/admin/sync/mappings/clear": {
      "post": {
        "description": "Remove a bad series↔external mapping; the next pull/push (or targeted push) re-resolves it\nfrom scratch.",
        "operationId": "clear_sync_mapping",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SyncMappingTarget"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "removed": true
                },
                "schema": {}
              }
            },
            "description": "Whether a mapping was actually removed"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Clear a series↔external mapping",
        "tags": [
          "admin-sync"
        ]
      }
    },
    "/v1/admin/sync/pull": {
      "post": {
        "description": "Operator-forced pull for another user's linked account. Response shape is defined by the\nsync service and forwarded verbatim; not tracked here.",
        "operationId": "admin_sync_pull",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SyncAccountTarget"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "Pulled, forwarded from the sync service"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Account not linked"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Force-pull another user's linked account",
        "tags": [
          "admin-sync"
        ]
      }
    },
    "/v1/admin/sync/push": {
      "post": {
        "description": "Operator-forced push for another user's linked account. Response shape is defined by the\nsync service and forwarded verbatim; not tracked here.",
        "operationId": "admin_sync_push",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SyncAccountTarget"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "Pushed, forwarded from the sync service"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Account not linked"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Force-push another user's linked account",
        "tags": [
          "admin-sync"
        ]
      }
    },
    "/v1/admin/sync/series/{id}": {
      "get": {
        "description": "Every external mapping recorded for one series, so the console can render a per-series\n\"manga info\" panel showing what it is (and is not) synced to.",
        "operationId": "list_sync_mappings_for_series",
        "parameters": [
          {
            "description": "Series id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/SeriesId"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/AdminMappingRow"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Mappings for this series"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "List a series' external mappings",
        "tags": [
          "admin-sync"
        ]
      }
    },
    "/v1/admin/sync/suggest": {
      "get": {
        "description": "Rank local catalogue series as likely matches for a fetched remote entry, so the operator\ngets automatic suggestions instead of blind-searching. Uses the same trigram candidates as\nauto-matching but returns the *full* ranked list (with scores) rather than only confident\nones, so even weak-but-plausible matches are offered.",
        "operationId": "list_suggestions",
        "parameters": [
          {
            "description": "The remote entry's title to match against the local catalogue.",
            "in": "query",
            "name": "title",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Optional local content-type token (`manga`/`manhwa`/…) to sharpen scoring.",
            "in": "query",
            "name": "content_type",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Optional release year to sharpen scoring.",
            "in": "query",
            "name": "start_year",
            "required": false,
            "schema": {
              "format": "int32",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/SuggestedMatch"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Up to 8 ranked suggestions, best score first"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Suggest local matches for a remote entry",
        "tags": [
          "admin-sync"
        ]
      }
    },
    "/v1/admin/sync/unlink": {
      "post": {
        "description": "Operator-forced unlink of another user's linked account. Response shape is defined by the\nsync service and forwarded verbatim; not tracked here.",
        "operationId": "admin_sync_unlink",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SyncAccountTarget"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "Unlinked, forwarded from the sync service"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Force-unlink another user's account",
        "tags": [
          "admin-sync"
        ]
      }
    },
    "/v1/admin/sync/unmapped": {
      "get": {
        "description": "The assign queue: canonical series without a mapping for the given provider, richest\nfirst, so operators can review and hand-assign the ones the automatic matcher was not\nconfident enough to link.",
        "operationId": "list_unmapped_series",
        "parameters": [
          {
            "description": "External provider to check membership against (e.g. `anilist`).",
            "in": "query",
            "name": "provider",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Optional case-insensitive title filter.",
            "in": "query",
            "name": "query",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/UnmappedSeriesRow"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Up to 100 unmapped series"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "provider is empty"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "List series without a mapping",
        "tags": [
          "admin-sync"
        ]
      }
    },
    "/v1/admin/sync/unmatched": {
      "get": {
        "description": "The reverse assign queue: remote provider entries a pull fetched but the auto-matcher\ncould not confidently link to a local series, so an operator can reconcile **every**\nloaded entry by hand (not just the confident matches).",
        "operationId": "list_unmatched_remote",
        "parameters": [
          {
            "description": "External provider to check membership against (e.g. `anilist`).",
            "in": "query",
            "name": "provider",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Optional case-insensitive title filter.",
            "in": "query",
            "name": "query",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/RemoteEntryRow"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Up to 200 unmatched remote entries"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "provider is empty"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "List unmatched remote entries",
        "tags": [
          "admin-sync"
        ]
      }
    },
    "/v1/admin/users": {
      "get": {
        "description": "The operator user directory: identity, account state, permission and tracking counts, and\nlast sign-in, with search and paging.",
        "operationId": "list_users",
        "parameters": [
          {
            "description": "Case-insensitive substring match on username or email. Empty lists everyone.",
            "in": "query",
            "name": "search",
            "required": false,
            "schema": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          {
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "format": "int64",
              "type": [
                "integer",
                "null"
              ]
            }
          },
          {
            "in": "query",
            "name": "offset",
            "required": false,
            "schema": {
              "format": "int64",
              "type": [
                "integer",
                "null"
              ]
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DirectoryPage"
                }
              }
            },
            "description": "A page of the user directory"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "List users",
        "tags": [
          "admin-users"
        ]
      }
    },
    "/v1/admin/users/{id}": {
      "delete": {
        "description": "Deletes the account and every row it owns, using the same cascade as self-service erasure\n(GDPR Art. 17) so there is one implementation rather than two that can diverge. The\naccount's audit records survive in pseudonymised form; see\n`tankovault_db::repo::privacy::erase_user`.\n\nIrreversible. Requires the username back, refuses the caller's own account, and refuses the\nlast active administrator.",
        "operationId": "delete_user",
        "parameters": [
          {
            "description": "User id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DeleteUser"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Account and all owned data erased"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "confirmation mismatch, own account, or the last administrator"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "no such user"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Erase a user",
        "tags": [
          "admin-users"
        ]
      },
      "get": {
        "description": "Everything the user-detail panel shows: identity, account state, activity counts, and the\npermissions currently granted with who granted each and when.",
        "operationId": "get_user",
        "parameters": [
          {
            "description": "User id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UserDetailResponse"
                }
              }
            },
            "description": "The account and its grants"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "no such user"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Get a user",
        "tags": [
          "admin-users"
        ]
      },
      "patch": {
        "description": "Change another account's username and/or email. Omitted fields are left alone.\n\nChanging an email does **not** re-open verification: an administrator setting an address is\nasserting it is correct, and forcing a confirmation round trip would lock the account out\nuntil the user acted on mail they did not expect.",
        "operationId": "update_user",
        "parameters": [
          {
            "description": "User id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AdminProfileUpdate"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UserDetailResponse"
                }
              }
            },
            "description": "The updated account"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "no such user"
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "email or username already taken"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Edit a user's identity",
        "tags": [
          "admin-users"
        ]
      }
    },
    "/v1/admin/users/{id}/permissions": {
      "put": {
        "description": "Replaces the account's entire grant set. The audit record names what was actually added and\nremoved, not the submitted list, so \"who gave them that\" is answerable afterwards.",
        "operationId": "set_user_permissions",
        "parameters": [
          {
            "description": "User id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetPermissions"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UserDetailResponse"
                }
              }
            },
            "description": "The updated account and its grants"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "cannot edit your own permissions, or strip the last administrator"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "no such user"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Set a user's permissions",
        "tags": [
          "admin-users"
        ]
      }
    },
    "/v1/admin/users/{id}/revoke-sessions": {
      "post": {
        "description": "Signs the account out of every device. Does not change what the account may do — use\nsuspension for that; this only ends the sessions currently in flight.",
        "operationId": "revoke_user_sessions",
        "parameters": [
          {
            "description": "User id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "revoked": 3
                },
                "schema": {}
              }
            },
            "description": "How many sessions were revoked"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Revoke a user's sessions",
        "tags": [
          "admin-users"
        ]
      }
    },
    "/v1/admin/users/{id}/status": {
      "post": {
        "description": "A suspended account cannot sign in, refresh a session, or take any action — the check\nhappens before authorization, so it is not something a permission can override. Nothing the\naccount owns is deleted, so the change is fully reversible.",
        "operationId": "set_user_status",
        "parameters": [
          {
            "description": "User id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetUserStatus"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UserDetailResponse"
                }
              }
            },
            "description": "The updated account"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "cannot suspend your own account, or the last administrator"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "no such user"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Suspend or reinstate a user",
        "tags": [
          "admin-users"
        ]
      }
    },
    "/v1/admin/users/{id}/verify-email": {
      "post": {
        "description": "The escape hatch for an account that never received its confirmation link. Without it, a\ndeployment whose outbound mail broke leaves those accounts permanently unable to sign in\nwith no self-service route back.",
        "operationId": "verify_user_email",
        "parameters": [
          {
            "description": "User id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UserDetailResponse"
                }
              }
            },
            "description": "The updated account"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "caller does not hold the required permission"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "no such user"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Confirm a user's email address",
        "tags": [
          "admin-users"
        ]
      }
    },
    "/v1/auth/login": {
      "post": {
        "description": "Authenticates by email or username + password. Issues an access token and a rotating\nrefresh-token cookie.\n\nEvery outcome — success, unknown identifier, bad password, unverified address — is\naudited. An authentication log that only records successes cannot answer the question\nanyone actually asks it after an incident.",
        "operationId": "login",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/LoginRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TokenResponse"
                }
              }
            },
            "description": "Authenticated; access token issued"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Invalid email/username or password"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Email address not yet confirmed"
          },
          "429": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Too many attempts; retry later"
          }
        },
        "summary": "Log in",
        "tags": [
          "auth"
        ]
      }
    },
    "/v1/auth/logout": {
      "post": {
        "description": "Revokes the presented refresh-token family (if any) and clears the cookie.",
        "operationId": "logout",
        "responses": {
          "200": {
            "description": "Logged out; refresh cookie cleared"
          }
        },
        "summary": "Log out",
        "tags": [
          "auth"
        ]
      }
    },
    "/v1/auth/passkey/login/finish": {
      "post": {
        "description": "Verifies the assertion against the challenge issued by\n[`passkey_login_start`](passkey_login_start) and, on success, issues an access token and a\nrotating refresh cookie — the same session a password sign-in produces.\n\nEvery outcome is audited under `auth.passkey_login`, matching\n[`login`](super::login::login): an authentication log that only records successes cannot\nanswer the question anyone asks it after an incident.",
        "operationId": "passkey_login_finish",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PasskeyLoginRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TokenResponse"
                }
              }
            },
            "description": "Authenticated; access token issued"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "The assertion did not verify, or the challenge is no longer live"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "The account is suspended, or its email is not yet confirmed"
          },
          "429": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Too many attempts; retry later"
          },
          "503": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "This deployment has no WebAuthn relying party configured"
          }
        },
        "summary": "Complete a passkey sign-in",
        "tags": [
          "auth"
        ]
      }
    },
    "/v1/auth/passkey/login/start": {
      "post": {
        "description": "Issues a discoverable-credential challenge. Deliberately unauthenticated and deliberately\nidentifier-free: the account is learned from the response, not asked for here.\n\nThe response is safe to hand to any caller. It contains a random challenge and no\ninformation about which accounts exist — mint one for a nonexistent user and it is\nindistinguishable from any other, because no user was named.",
        "operationId": "passkey_login_start",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PasskeyChallenge"
                }
              }
            },
            "description": "Challenge issued"
          },
          "429": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Too many attempts; retry later"
          },
          "503": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "This deployment has no WebAuthn relying party configured"
          }
        },
        "summary": "Begin a passkey sign-in",
        "tags": [
          "auth"
        ]
      }
    },
    "/v1/auth/password/forgot": {
      "post": {
        "description": "Always responds `202 Accepted`, whether or not the address is registered, so the\nendpoint can't be used to probe which emails have accounts. When the address does exist\nand email is configured, a single-use, time-limited reset link is sent.\n\n**Every** account-dependent step happens on a detached task (SEC-10). The uniform `202`\nwas previously defeated by timing: the known-address branch generated a token, hashed it\nand performed an `INSERT` — a write, so a WAL flush — before answering, while the unknown\nbranch returned straight after the lookup. That is a smaller gap than `login`'s two orders\nof magnitude but it is the same oracle, and it needs no credentials at all to read. With\nthe whole body spawned, this handler's only statement is the spawn, so there is no branch\nleft for a response time to disclose.\n\nThe return type is deliberately `StatusCode` rather than `ApiResult<StatusCode>`: the\nanti-enumeration property is \"this endpoint has exactly one answer\", and making that a\ntype rather than a convention means a later edit cannot reintroduce a second one.",
        "operationId": "forgot_password",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ForgotPasswordRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "202": {
            "description": "If the address is registered, a reset email has been sent"
          }
        },
        "summary": "Request a password-reset email",
        "tags": [
          "auth"
        ]
      }
    },
    "/v1/auth/password/reset": {
      "post": {
        "description": "Consumes a valid, unexpired, unused reset token, sets the new password, and revokes all\nof the user's active sessions so any stolen refresh token dies with the old credential.",
        "operationId": "reset_password",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ResetPasswordRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Password changed; existing sessions revoked"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Invalid/expired token or weak password"
          }
        },
        "summary": "Reset a password with a token",
        "tags": [
          "auth"
        ]
      }
    },
    "/v1/auth/refresh": {
      "post": {
        "description": "Reads the `refresh_token` `HttpOnly` cookie, rotates it, and issues a fresh access token.\nPresenting a token that was already rotated (reuse) revokes the whole token family. The one\nexception is a token presented within a short grace window of its own rotation while its\nfamily is still live: that is an interrupted or raced rotation by a client that never took\ndelivery of the successor, and it is served — collapsing the family to the single token it\nissues — rather than counted as theft.",
        "operationId": "refresh",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TokenResponse"
                }
              }
            },
            "description": "Refreshed; new access token issued"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Missing, expired, or already-rotated refresh token"
          }
        },
        "summary": "Refresh the access token",
        "tags": [
          "auth"
        ]
      }
    },
    "/v1/auth/register": {
      "post": {
        "description": "Validates the request and creates the user. When email delivery is configured the account\nis created **unconfirmed** and a confirmation link is emailed — the user must click it\nbefore they can sign in, and no session is issued. When email is not configured the\naccount is activated immediately and a session is issued exactly like [`login`], so\ndevelopment and SMTP-less self-hosting keep working.",
        "operationId": "register",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RegisterRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RegisterResponse"
                }
              }
            },
            "description": "Account created; either a confirmation email was sent or (email unconfigured) an access token was issued"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Invalid email, username or password"
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Email or username already taken"
          }
        },
        "summary": "Register a new account",
        "tags": [
          "auth"
        ]
      }
    },
    "/v1/auth/verify-email": {
      "post": {
        "description": "Consumes a valid, unexpired, unused confirmation token, marks the address verified, and\nsigns the user in — issuing an access token and a rotating refresh cookie exactly like\n[`login`] so clicking the link lands the user in the app.",
        "operationId": "verify_email",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/VerifyEmailRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TokenResponse"
                }
              }
            },
            "description": "Email confirmed; session issued"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Invalid or expired confirmation token"
          }
        },
        "summary": "Confirm an email address with a token",
        "tags": [
          "auth"
        ]
      }
    },
    "/v1/auth/verify-email/resend": {
      "post": {
        "description": "Always responds `202 Accepted`, whether or not the address is registered or already\nconfirmed, so the endpoint can't be used to probe which emails have accounts. A fresh\nlink is only sent when the address exists, is still unconfirmed, and email is configured.\n\nSpawned in full, for the reason `auth::password::forgot_password` explains at length\n(SEC-10). This endpoint's channel was in fact the wider of the two: the known-and-unconfirmed\nbranch performed the token `INSERT`, while both \"no such address\" *and* \"already confirmed\"\nreturned straight after the lookup — so the timing separated three states rather than two,\nand \"this address exists and has not been confirmed\" is the more useful answer of the pair.\nThe audit named `forgot_password`; this is the same defect in the sibling handler.",
        "operationId": "resend_verification",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ResendVerificationRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "202": {
            "description": "If the address is registered and unconfirmed, a confirmation email has been sent"
          }
        },
        "summary": "Resend the email-confirmation link",
        "tags": [
          "auth"
        ]
      }
    },
    "/v1/legal": {
      "get": {
        "description": "Only what this deployment actually publishes. An operator who configures no Imprint gets no\nImprint entry, so the footer renders no dead link rather than one that 404s.",
        "operationId": "legal_index",
        "parameters": [
          {
            "description": "Language code (`en`, `de`). Falls back to `Accept-Language`, then to the first locale\nthe operator configured.",
            "in": "query",
            "name": "lang",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/LegalIndexEntry"
                  },
                  "type": "array"
                }
              }
            },
            "description": "The configured documents"
          }
        },
        "summary": "List the legal documents",
        "tags": [
          "legal"
        ]
      }
    },
    "/v1/legal/{slug}": {
      "get": {
        "description": "The Markdown as the operator wrote it. Rendering — and sanitising the result — is the\nclient's job: this is operator input, not developer input, so it is never trusted as HTML.",
        "operationId": "legal_document",
        "parameters": [
          {
            "description": "Document slug",
            "in": "path",
            "name": "slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Language code (`en`, `de`). Falls back to `Accept-Language`, then to the first locale\nthe operator configured.",
            "in": "query",
            "name": "lang",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LegalDocumentView"
                }
              }
            },
            "description": "The document in the served locale"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "no such document, or its file is unreadable"
          }
        },
        "summary": "Get one legal document",
        "tags": [
          "legal"
        ]
      }
    },
    "/v1/me": {
      "delete": {
        "description": "Erases the account and every row owned by it (GDPR Art. 17, right to erasure).\n\nAudit records of privileged actions the user performed are **retained in pseudonymised\nform**: the actor reference is nulled, so the record of what happened survives while\nthe link to a person does not. Documented on\n`tankovault_db::repo::privacy::erase_user`.\n\nAny of the user's own open data-subject requests are resolved as completed *before* the\ncascade, so the compliance record shows the erasure was carried out rather than leaving a\nrequest that appears abandoned.",
        "operationId": "delete_account",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DeleteAccount"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Account and all owned data erased"
          },
          "400": {
            "description": "Confirmation did not match"
          },
          "401": {
            "description": "Unauthenticated"
          },
          "404": {
            "description": "self-service deletion is switched off; file an erasure request instead"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Delete my account",
        "tags": [
          "me-account"
        ]
      }
    },
    "/v1/me/capabilities": {
      "get": {
        "description": "The permissions the caller holds and the features this deployment has enabled — everything\na client needs to decide which navigation, panels and controls to render.\n\nNot cacheable: a permission revoked or a feature switched off must be reflected on the next\nfetch, and this is the endpoint a client polls to find out.",
        "operationId": "capabilities",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Capabilities"
                }
              }
            },
            "description": "Held permissions and enabled features"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "the account is suspended"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Get my capabilities",
        "tags": [
          "me-account"
        ]
      }
    },
    "/v1/me/continue": {
      "get": {
        "description": "Continue-reading cards for Home / the Series CTA (frontend §9.3): tracked, in-progress\nseries that have unread chapters, freshest activity first.",
        "operationId": "continue_reading",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/ContinueItem"
                  },
                  "type": "array"
                }
              }
            },
            "description": "All continue-reading cards"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Get continue-reading cards",
        "tags": [
          "me-dashboard"
        ]
      }
    },
    "/v1/me/export": {
      "get": {
        "description": "Everything the system holds about the authenticated user, as a single JSON document\n(GDPR Art. 20, right to data portability).\n\nServed as an attachment rather than an inline body: the response is the user's entire\npersonal record and a browser should offer to save it rather than render it.\nCredentials — password hash, session token hashes, third-party OAuth tokens — are\nexcluded; see `tankovault_db::repo::privacy::export_user_data`.",
        "operationId": "export_data",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "Complete personal-data export"
          },
          "401": {
            "description": "Unauthenticated"
          },
          "404": {
            "description": "self-service export is switched off; file an access request instead"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Export my data",
        "tags": [
          "me-account"
        ]
      }
    },
    "/v1/me/feed": {
      "get": {
        "description": "Unread chapters across the watchlist (the reading dashboard).",
        "operationId": "feed",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/FeedEntry"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Up to 100 most recent unread chapters"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Get the unread-chapters feed",
        "tags": [
          "me-dashboard"
        ]
      }
    },
    "/v1/me/notification-prefs": {
      "get": {
        "description": "The caller's notification preferences JSON (frontend §9.4). `{}` means \"product defaults\".",
        "operationId": "notification_prefs",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "Product-defined free-form preferences JSON"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Get notification preferences",
        "tags": [
          "me-notifications"
        ]
      },
      "put": {
        "description": "Replace the caller's notification preferences (frontend §9.4). The body is stored verbatim\nas an open JSON document.",
        "operationId": "put_notification_prefs",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {}
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "The stored preferences, echoed back"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Replace notification preferences",
        "tags": [
          "me-notifications"
        ]
      }
    },
    "/v1/me/notifications": {
      "get": {
        "description": "A page of the caller's inbox, newest first, with the inbox-wide `total` and `unread`.\n\nThe body is an object rather than the bare array it used to be, for the same reason\n`/v1/me/watchlist` is: neither count is derivable from a page of items, and the frontend —\nthe only consumer, and regenerated from this document — needs both to page and to keep the\nbell honest past the first page.",
        "operationId": "notifications",
        "parameters": [
          {
            "description": "Page size, clamped to `1..=200`.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "format": "int64",
              "type": "integer"
            }
          },
          {
            "in": "query",
            "name": "offset",
            "required": false,
            "schema": {
              "format": "int64",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/NotificationsView"
                }
              }
            },
            "description": "A page of the caller's notifications"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "List notifications",
        "tags": [
          "me-notifications"
        ]
      }
    },
    "/v1/me/notifications/read": {
      "post": {
        "operationId": "mark_read",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MarkRead"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "marked": 3
                },
                "schema": {}
              }
            },
            "description": "Number of notifications marked read"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Mark notifications read",
        "tags": [
          "me-notifications"
        ]
      }
    },
    "/v1/me/passkeys": {
      "get": {
        "description": "Every passkey registered to the caller, newest first.",
        "operationId": "list_passkeys",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/PasskeyDto"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Registered passkeys"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "List my passkeys",
        "tags": [
          "me-account"
        ]
      }
    },
    "/v1/me/passkeys/register/finish": {
      "post": {
        "description": "Verifies the attestation against the challenge issued by [`passkey_register_start`] and\nstores the credential.",
        "operationId": "passkey_register_finish",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PasskeyRegisterFinish"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PasskeyDto"
                }
              }
            },
            "description": "Registered"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Malformed credential"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required, or the challenge is no longer live"
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "This authenticator is already registered"
          },
          "503": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "This deployment has no WebAuthn relying party configured"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Finish registering a passkey",
        "tags": [
          "me-account"
        ]
      }
    },
    "/v1/me/passkeys/register/start": {
      "post": {
        "description": "Issues a `WebAuthn` creation challenge for the caller's account.\n\n# Why this asks for the password\n\nBecause a passkey is a **permanent** credential, and an access token is a 15-minute one.\nWithout this check, anyone who got hold of a token for those fifteen minutes — a shared\nbrowser, a proxy log, an XSS payload that survives one page — could register their own\nauthenticator and keep the account forever, surviving every password change and session\nrevocation the real owner performs. That is a strictly worse version of the takeover\n`patch_profile` blocks by requiring the password before an email change, and it is the same\nanswer: installing a credential requires proving you hold one.\n\nThe cost is that a user who signed in *with* a passkey must type their password to add a\nsecond one. That is the trade every major implementation makes, and the friction lands on a\nonce-per-device action rather than on sign-in.\n\nCredentials already registered to the account are sent as the exclusion list, so an\nauthenticator that already holds one for this account says so at the prompt instead of\nsilently minting an indistinguishable duplicate.",
        "operationId": "passkey_register_start",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PasskeyRegisterStart"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PasskeyRegisterChallenge"
                }
              }
            },
            "description": "Challenge issued"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "The label is too long"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required, or current_password is wrong"
          },
          "503": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "This deployment has no WebAuthn relying party configured"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Begin registering a passkey",
        "tags": [
          "me-account"
        ]
      }
    },
    "/v1/me/passkeys/{id}": {
      "delete": {
        "description": "Delete one of the caller's own passkeys. Scoped to ownership; a foreign or unknown id yields\n`404`.\n\nDeleting the last passkey is allowed, and is not a lockout: every account on this deployment\nhas a password, and passkeys are additive to it. Refusing would be the wrong protection\nanyway — the case a user most urgently needs this for is a device they have just lost.",
        "operationId": "delete_passkey",
        "parameters": [
          {
            "description": "Passkey id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Revoked"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "No such passkey for this caller"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Revoke a passkey",
        "tags": [
          "me-account"
        ]
      },
      "patch": {
        "description": "Change the label on one of the caller's own passkeys. Scoped to ownership; a foreign or\nunknown id yields `404`.",
        "operationId": "rename_passkey",
        "parameters": [
          {
            "description": "Passkey id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PasskeyRename"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Renamed"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "The label is too long"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "No such passkey for this caller"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Rename a passkey",
        "tags": [
          "me-account"
        ]
      }
    },
    "/v1/me/password": {
      "post": {
        "description": "Change the caller's password, proving knowledge of the current one.\n\nThere was previously **no authenticated path to a new password at all** — the only route\nwas the emailed reset link, so a signed-in user who simply wanted to rotate their password\nhad to go through an out-of-band channel, and a user whose email had been taken over could\nnot lock the attacker out.\n\nEvery session is revoked on success, including the caller's: a password change is exactly\nwhen you want the other device signed out, and leaving the caller's own session alive would\nmean special-casing the one session an attacker is most likely to be holding.",
        "operationId": "change_password",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PasswordChange"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Password changed; every session was revoked"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "New password fails the policy"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required, or current_password is wrong"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Change the password",
        "tags": [
          "me-account"
        ]
      }
    },
    "/v1/me/privacy/requests": {
      "get": {
        "description": "The authenticated account's own requests, newest first, including resolved ones — the\nsubject is entitled to see how their requests were handled, not only which are outstanding.",
        "operationId": "list_privacy_requests",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/RequestRow"
                  },
                  "type": "array"
                }
              }
            },
            "description": "The caller's requests"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "the data-subject request queue is switched off"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "List my data-subject requests",
        "tags": [
          "me-account"
        ]
      },
      "post": {
        "description": "Opens a tracked request against the authenticated account and returns it with its Art. 12(3)\ndeadline, which is the one thing the subject is entitled to know up front.\n\nOne open request per kind: a second would start its own clock and show the queue two\ndeadlines for one obligation.",
        "operationId": "create_privacy_request",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/NewPrivacyRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RequestRow"
                }
              }
            },
            "description": "The filed request, with its response deadline"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "rectification without detail, or a duplicate open request"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "the data-subject request queue is switched off"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "File a data-subject request",
        "tags": [
          "me-account"
        ]
      }
    },
    "/v1/me/privacy/requests/{id}": {
      "delete": {
        "description": "Cancels one of the caller's own open requests. Scoped to ownership, so the id alone is not\nauthority to cancel someone else's, and refused once resolved — a request that has already\nbeen answered is a compliance record, not a draft.",
        "operationId": "cancel_privacy_request",
        "parameters": [
          {
            "description": "Request id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Withdrawn"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "no such open request for this caller"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Withdraw a data-subject request",
        "tags": [
          "me-account"
        ]
      }
    },
    "/v1/me/profile": {
      "patch": {
        "description": "Update the caller's username and/or email (frontend §9.4). A duplicate email/username\nsurfaces as `409 Conflict`.\n\nChanging the **email address** additionally requires `current_password`, because the\naddress is the account's recovery channel. Without that check, anyone holding an access\ntoken for 15 minutes — a shared browser, a proxy log, a leaked SSE URL — could point the\naccount at their own address, request a password reset to it, and take the account over;\n`reset_password` would then revoke the real owner's sessions on the attacker's behalf.\n\nOn a successful change the new address starts **unverified**\n(`repo::users::update_profile` clears `email_verified_at`), a confirmation link is sent to\nit, a warning is sent to the *old* address, and every session is revoked — matching what\n`reset_password` already does for the other credential.",
        "operationId": "patch_profile",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ProfileUpdate"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProfileDto"
                }
              }
            },
            "description": "Updated profile"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Invalid username or email"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required, or current_password is wrong"
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Email or username already taken"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Update the profile",
        "tags": [
          "me-account"
        ]
      }
    },
    "/v1/me/progress/bulk-read": {
      "post": {
        "description": "Backs the Watchlist's `Mark group read`: each listed series' progress advances to its\nhighest known chapter. Ids the caller does not track come back in `skipped`.\n\nThe frontier only moves forward, so pressing this twice is a no-op the second time, and a\nseries whose catalogue has shrunk is not rewound.",
        "operationId": "bulk_mark_read",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/WatchlistBulkIds"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/BulkResult"
                }
              }
            },
            "description": "Per-id outcome"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "empty or oversized id list"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Mark every chapter read across many series",
        "tags": [
          "me-progress"
        ]
      }
    },
    "/v1/me/progress/{series_id}": {
      "get": {
        "description": "Both read frontiers for the series (design v2 §A.6).",
        "operationId": "get_progress",
        "parameters": [
          {
            "description": "Series id",
            "in": "path",
            "name": "series_id",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/SeriesId"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProgressDto"
                }
              }
            },
            "description": "Progress (defaults to zero when untracked)"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Get read progress",
        "tags": [
          "me-progress"
        ]
      },
      "put": {
        "description": "Set the whole-chapter frontier outright (design v2 §A.6).",
        "operationId": "put_progress",
        "parameters": [
          {
            "description": "Series id",
            "in": "path",
            "name": "series_id",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/SeriesId"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ProgressUpdate"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "ok": true
                },
                "schema": {}
              }
            },
            "description": "Acknowledged"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Set the read-progress frontier",
        "tags": [
          "me-progress"
        ]
      }
    },
    "/v1/me/progress/{series_id}/chapters/{number}": {
      "put": {
        "description": "Apply the §A.3 mark-read/mark-unread rule for one chapter number. Unmarking an older\n(non-frontier) chapter retreats progress past it too; the client must confirm with the user\nfirst in that case (design v2 §A.6).",
        "operationId": "put_chapter_progress",
        "parameters": [
          {
            "description": "Series id",
            "in": "path",
            "name": "series_id",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/SeriesId"
            }
          },
          {
            "description": "Chapter number",
            "in": "path",
            "name": "number",
            "required": true,
            "schema": {
              "format": "double",
              "type": "number"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ChapterRead"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "ok": true
                },
                "schema": {}
              }
            },
            "description": "Acknowledged"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Mark a single chapter read or unread",
        "tags": [
          "me-progress"
        ]
      }
    },
    "/v1/me/progress/{series_id}/mark-read-to": {
      "post": {
        "description": "\"Mark read to here\" (design v2 §A.6); equivalent to marking `number` read.",
        "operationId": "mark_read_to",
        "parameters": [
          {
            "description": "Series id",
            "in": "path",
            "name": "series_id",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/SeriesId"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MarkReadTo"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "ok": true
                },
                "schema": {}
              }
            },
            "description": "Acknowledged"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Mark read up to a chapter",
        "tags": [
          "me-progress"
        ]
      }
    },
    "/v1/me/recommendations": {
      "get": {
        "description": "A personalised shelf: content-similar to what the reader has finished or is deep into,\nfiltered against what they already track or have refused, diversified, and explained.\n\nFalls back to the catalogue's popularity prior for a reader with no history, and returns an\nempty array when the recommendation model has never been built.",
        "operationId": "recommendations",
        "parameters": [
          {
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "format": "int64",
              "type": [
                "integer",
                "null"
              ]
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/Recommendation"
                  },
                  "type": "array"
                }
              }
            },
            "description": "The reader's shelf, best first"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Get \"because you read\" recommendations",
        "tags": [
          "me-dashboard"
        ]
      }
    },
    "/v1/me/recommendations/{series_id}/feedback": {
      "post": {
        "description": "Records that the reader does not want this series suggested. `not_interested` suppresses it\nfor ninety days; `hide_forever` does not expire. A stronger refusal is never softened by a\nlater weaker one.",
        "operationId": "feedback",
        "parameters": [
          {
            "description": "Series to suppress",
            "in": "path",
            "name": "series_id",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/SeriesId"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/FeedbackBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Recorded"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "unknown verdict"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Dismiss a recommendation",
        "tags": [
          "me-dashboard"
        ]
      }
    },
    "/v1/me/sessions": {
      "get": {
        "description": "The caller's active login sessions (frontend §9.4).",
        "operationId": "sessions",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/SessionDto"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Active sessions"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "List active sessions",
        "tags": [
          "me-account"
        ]
      }
    },
    "/v1/me/sessions/{id}": {
      "delete": {
        "description": "Revoke one of the caller's own sessions (frontend §9.4). Scoped to ownership; a\nforeign/unknown id yields `404`.",
        "operationId": "delete_session",
        "parameters": [
          {
            "description": "Session id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "revoked": 1
                },
                "schema": {}
              }
            },
            "description": "Revoked"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "No such session for this caller"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Revoke a session",
        "tags": [
          "me-account"
        ]
      }
    },
    "/v1/me/stats": {
      "get": {
        "description": "*Stub*: lifetime tracking stats for the Home / Profile headline (frontend §9.3). See\n[`tankovault_db::repo::tracking::MeStats`] for the honest definition of `chapters_read` and\nwhy no \"streak\" is returned.",
        "operationId": "stats",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MeStats"
                }
              }
            },
            "description": "Lifetime stats"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Get lifetime tracking stats",
        "tags": [
          "me-dashboard"
        ]
      }
    },
    "/v1/me/stream": {
      "get": {
        "description": "Server-Sent Events of live per-user notifications (design §14, §17.4). Authenticated by a\nsingle-use `ticket` query parameter obtained from `POST /v1/me/stream-ticket` — not the\n`Authorization` header, which `EventSource` cannot set. It subscribes to the ticket's own\nuser's core-NATS subject and relays each `UserNotification` as a `notification` SSE event,\nwith a periodic keep-alive comment so proxies keep the connection open. Ownership is\nimplicit: the subscription is scoped to the user the ticket was minted for.\n\nThe ticket is consumed here, so `EventSource`'s *automatic* reconnect cannot re-open this\nstream — the client has to mint a new ticket per attempt (`web/frontend/src/live.rs` does).\nThat is a feature rather than a cost: re-minting goes through `AuthUser`, so a suspension\napplied mid-stream is caught by the mint call as well as by the check below.",
        "operationId": "stream",
        "parameters": [
          {
            "description": "Single-use ticket from `POST /v1/me/stream-ticket`, passed as a query parameter because\nthe browser `EventSource` API cannot attach an `Authorization` header (design §17.4).\n\nWas the raw access token until SEC-8. A query string is recorded by `TraceLayer` as a\nspan field, preserved verbatim by the frontend proxy, written to every reverse-proxy\naccess log and kept in browser history — so the credential that rides here must be worth\nnothing by the time anyone reads it back. This one is spent by the request that carries\nit, expires in 30 seconds, and opens nothing but this stream.",
            "in": "query",
            "name": "ticket",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "text/event-stream": {}
            },
            "description": "SSE stream of `notification` events"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "missing, expired, or already-redeemed ticket"
          },
          "503": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "the live notification stream is temporarily unavailable"
          }
        },
        "security": [
          {
            "stream_ticket": []
          }
        ],
        "summary": "Live notification stream",
        "tags": [
          "me-notifications"
        ]
      }
    },
    "/v1/me/stream-ticket": {
      "post": {
        "description": "Exchanges the caller's `Bearer` session for a single-use, 30-second credential that\n`EventSource` can carry in a query string. Mint one per connection attempt — including each\nreconnect, since redeeming a ticket spends it.",
        "operationId": "stream_ticket",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StreamTicket"
                }
              }
            },
            "description": "A single-use ticket for `GET /v1/me/stream`"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "503": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "the ticket store is temporarily unavailable"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Mint a stream ticket",
        "tags": [
          "me-notifications"
        ]
      }
    },
    "/v1/me/sync/conflicts": {
      "get": {
        "description": "The caller's pending conflicts across all providers (§B.6). Rows are\n`tankovault_contracts::sync::ConflictView`, the shape the sync service publishes.",
        "operationId": "sync_conflicts",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/ConflictRow"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Pending conflicts, forwarded from the sync service"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "List pending sync conflicts",
        "tags": [
          "me-sync"
        ]
      }
    },
    "/v1/me/sync/conflicts/{id}/resolve": {
      "post": {
        "description": "Apply the caller's chosen resolution (§B.6). Response shape is defined by the sync service\nand forwarded verbatim; not tracked here.",
        "operationId": "sync_resolve_conflict",
        "parameters": [
          {
            "description": "Conflict id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ResolveConflict"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "Resolved, forwarded from the sync service"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "No such conflict"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Resolve a sync conflict",
        "tags": [
          "me-sync"
        ]
      }
    },
    "/v1/me/sync/history": {
      "get": {
        "description": "A page of the caller's sync history (§B.6). Rows are\n`tankovault_contracts::sync::HistoryView`, the shape the sync service publishes.",
        "operationId": "sync_history",
        "parameters": [
          {
            "in": "query",
            "name": "series_id",
            "required": false,
            "schema": {
              "format": "uuid",
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "provider",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "page",
            "required": false,
            "schema": {
              "format": "int64",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/HistoryRow"
                  },
                  "type": "array"
                }
              }
            },
            "description": "A page of sync history, forwarded from the sync service"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Get sync history",
        "tags": [
          "me-sync"
        ]
      }
    },
    "/v1/me/sync/providers": {
      "get": {
        "description": "The registered external providers (design: generalized multi-provider sync). Drives the\nAccount \"Sync & integrations\" panel, which renders one card per entry instead of a single\nhardcoded `AniList` block. The body type is shared with the sync service via\n`tankovault_contracts::sync`, so the generated client exposes this endpoint typed.",
        "operationId": "sync_providers",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/ProviderInfo"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Providers, forwarded from the sync service"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "List sync providers",
        "tags": [
          "me-sync"
        ]
      }
    },
    "/v1/me/sync/{provider}": {
      "delete": {
        "description": "Unlink the caller's account at `provider`. Response shape is defined by the sync service\nand forwarded verbatim; not tracked here.",
        "operationId": "sync_disconnect",
        "parameters": [
          {
            "description": "Provider slug",
            "in": "path",
            "name": "provider",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "Unlinked, forwarded from the sync service"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Unlink a provider",
        "tags": [
          "me-sync"
        ]
      }
    },
    "/v1/me/sync/{provider}/authorize": {
      "get": {
        "description": "Returns the provider's consent URL (proxied). The body type is shared with the sync\nservice via `tankovault_contracts::sync`.",
        "operationId": "sync_authorize_url",
        "parameters": [
          {
            "description": "Provider slug",
            "in": "path",
            "name": "provider",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AuthorizeUrl"
                }
              }
            },
            "description": "Consent URL, forwarded from the sync service"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Get a provider's OAuth consent URL",
        "tags": [
          "me-sync"
        ]
      }
    },
    "/v1/me/sync/{provider}/callback": {
      "get": {
        "description": "Exchanges the authorization `code` and links the caller's account at `provider`. Response\nshape is defined by the sync service and forwarded verbatim; not tracked here.",
        "operationId": "sync_callback",
        "parameters": [
          {
            "description": "Provider slug",
            "in": "path",
            "name": "provider",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "code",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "Linked, forwarded from the sync service"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Unknown provider"
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Account not linked"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Complete an OAuth link",
        "tags": [
          "me-sync"
        ]
      }
    },
    "/v1/me/sync/{provider}/pull": {
      "post": {
        "description": "Import `provider`'s list into the local watchlist. Response shape is defined by the sync\nservice and forwarded verbatim; not tracked here.",
        "operationId": "sync_pull",
        "parameters": [
          {
            "description": "Provider slug",
            "in": "path",
            "name": "provider",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/components/schemas/SyncOpts"
                  }
                ]
              }
            }
          },
          "description": "Optional sync options; omitted body uses the service default"
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "Pulled, forwarded from the sync service"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Account not linked"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Pull a provider's list into local state",
        "tags": [
          "me-sync"
        ]
      }
    },
    "/v1/me/sync/{provider}/push": {
      "post": {
        "description": "Reflect local watchlist/progress to `provider` (bulk, full-reconciliation walk — see\n`spawn_targeted_push` for the fast per-series path used automatically when marking a\nchapter/series read). Response shape is defined by the sync service and forwarded\nverbatim; not tracked here.",
        "operationId": "sync_push",
        "parameters": [
          {
            "description": "Provider slug",
            "in": "path",
            "name": "provider",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/components/schemas/SyncOpts"
                  }
                ]
              }
            }
          },
          "description": "Optional sync options; omitted body uses the service default"
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "Pushed, forwarded from the sync service"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Account not linked"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Push local state to a provider",
        "tags": [
          "me-sync"
        ]
      }
    },
    "/v1/me/sync/{provider}/settings": {
      "get": {
        "description": "The caller's automatic-sync settings (design v2 §B.6). The body type is shared with the\nsync service via `tankovault_contracts::sync`.",
        "operationId": "sync_settings",
        "parameters": [
          {
            "description": "Provider slug",
            "in": "path",
            "name": "provider",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AccountSettings"
                }
              }
            },
            "description": "Settings, forwarded from the sync service"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "No settings for this provider"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Get automatic-sync settings",
        "tags": [
          "me-sync"
        ]
      },
      "patch": {
        "description": "Update automatic sync + conflict policy (design v2 §B.6).",
        "operationId": "sync_settings_patch",
        "parameters": [
          {
            "description": "Provider slug",
            "in": "path",
            "name": "provider",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SyncSettingsPatch"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "ok": true
                },
                "schema": {}
              }
            },
            "description": "Acknowledged"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Update automatic-sync settings",
        "tags": [
          "me-sync"
        ]
      }
    },
    "/v1/me/sync/{provider}/status": {
      "get": {
        "description": "Whether the caller has a linked account at `provider`, plus the connected display name and\nmost recent sync time (Sync & integrations panel, header pill, Series tracking card).\nAlways `200`; an unlinked account reads `{ \"linked\": false }`. The body type is shared with\nthe sync service via `tankovault_contracts::sync`.",
        "operationId": "sync_status",
        "parameters": [
          {
            "description": "Provider slug",
            "in": "path",
            "name": "provider",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SyncAccountStatus"
                }
              }
            },
            "description": "Link status, forwarded from the sync service"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Get link status for a provider",
        "tags": [
          "me-sync"
        ]
      }
    },
    "/v1/me/taste": {
      "get": {
        "description": "What the recommender believes about this reader, in their own terms. Exists so the profile is\ninspectable by the person it describes — and so a bad shelf can be diagnosed without anyone\nreading a watchlist.",
        "operationId": "taste",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TasteView"
                }
              }
            },
            "description": "The reader's profile"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Get the reader's taste profile",
        "tags": [
          "me-dashboard"
        ]
      }
    },
    "/v1/me/watchlist": {
      "get": {
        "description": "The user's watchlist, filtered/sorted/paginated **server-side**, with the tab counts and\ngroup-header aggregates the list renders around it.\n\nThe body is an object rather than the bare `WatchlistItem[]` it used to be. That is a\nbreaking change, made deliberately: `total`, `counts` and `groups` cannot ride on response\nheaders without splitting one render into three requests, and the counts in particular are\n*not* derivable from `items` — they describe the tabs the caller is not looking at. The\nfrontend is the only consumer, and it is regenerated from this document.",
        "operationId": "watchlist",
        "parameters": [
          {
            "description": "`reading | planned | paused | completed | dropped`; absent means every status.",
            "in": "query",
            "name": "status",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Free-text filter over title, alternative titles, tags and authors.",
            "in": "query",
            "name": "q",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "unread_only",
            "required": false,
            "schema": {
              "type": "boolean"
            }
          },
          {
            "description": "`24h | 7d | 30d`; absent (or `any`) means no recency constraint.\n\nA *window* token rather than an absolute instant, despite the name. The client would\nhave to compute the instant from its own clock, and a browser minutes ahead of the\nserver would filter out the very releases the \"last 24 hours\" option exists to show.\nThe server resolves the window against the clock the timestamps were written by.",
            "in": "query",
            "name": "released_since",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Only rows whose preferred source is unhealthy.",
            "in": "query",
            "name": "source_issues",
            "required": false,
            "schema": {
              "type": "boolean"
            }
          },
          {
            "description": "`released | unread | added | title | progress` (default `released`).",
            "in": "query",
            "name": "sort",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "`asc | desc`. Defaults to whichever direction reads as \"most interesting first\" for the\nchosen sort — descending everywhere except `title`.",
            "in": "query",
            "name": "order",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "format": "int64",
              "type": "integer"
            }
          },
          {
            "in": "query",
            "name": "offset",
            "required": false,
            "schema": {
              "format": "int64",
              "type": "integer"
            }
          },
          {
            "description": "Opaque token from a previous response's `next_cursor`; supersedes `offset`.\n\nKeyset, not offset: the watchlist is live — a chapter discovered between two requests\nreorders the default sort — and an `OFFSET` page over a shifting list repeats one row\nand skips another. `offset` stays accepted for callers that refetch a whole prefix\nrather than appending, which is immune to the same problem by construction.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WatchlistView"
                }
              }
            },
            "description": "A page of the caller's watchlist"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "unrecognised filter or sort token"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Get the watchlist",
        "tags": [
          "me-watchlist"
        ]
      }
    },
    "/v1/me/watchlist/bulk": {
      "delete": {
        "operationId": "bulk_remove_watchlist",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/WatchlistBulkIds"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/BulkResult"
                }
              }
            },
            "description": "Per-id outcome"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "empty or oversized id list"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Remove many watchlist entries at once",
        "tags": [
          "me-watchlist"
        ]
      },
      "post": {
        "description": "Applies a status and/or notify change across a selection. Ids the caller does not track are\nreported in `skipped` rather than inserted — the selection was made from the list, so an id\nthat is not on it means the client is stale, and re-adding a title the user just removed is\nthe wrong repair.",
        "operationId": "bulk_update_watchlist",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/WatchlistBulkUpdate"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/BulkResult"
                }
              }
            },
            "description": "Per-id outcome"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "empty or oversized id list"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Change many watchlist entries at once",
        "tags": [
          "me-watchlist"
        ]
      }
    },
    "/v1/me/watchlist/summary": {
      "get": {
        "description": "Per-status counts and the unread total over the **whole** watchlist, under no filters.\n\nSeparate from the list's `counts`, which drop only the `status` arm and keep the search,\nrecency and source filters: those answer \"how many would this tab show given what I have\ntyped\", while this answers \"how big is my library\" for surfaces that carry no filter state\nof their own — a tab badge, the More sheet, a signed-in header.",
        "operationId": "watchlist_summary",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WatchlistSummaryView"
                }
              }
            },
            "description": "Counts across the whole watchlist"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Get the watchlist summary",
        "tags": [
          "me-watchlist"
        ]
      }
    },
    "/v1/me/watchlist/{series_id}": {
      "delete": {
        "operationId": "delete_watchlist",
        "parameters": [
          {
            "description": "Series id",
            "in": "path",
            "name": "series_id",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/SeriesId"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "ok": true
                },
                "schema": {}
              }
            },
            "description": "Acknowledged"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Remove a watchlist entry",
        "tags": [
          "me-watchlist"
        ]
      },
      "get": {
        "description": "The same enriched row the list renders, for a single series. The Series page used to answer\nthis by fetching the entire watchlist and scanning it — which stopped working the moment the\nlist paginated.",
        "operationId": "get_watchlist_entry",
        "parameters": [
          {
            "description": "Series id",
            "in": "path",
            "name": "series_id",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/SeriesId"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WatchlistEntryView"
                }
              }
            },
            "description": "The entry, or null when untracked"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Get one watchlist entry",
        "tags": [
          "me-watchlist"
        ]
      },
      "put": {
        "operationId": "put_watchlist",
        "parameters": [
          {
            "description": "Series id",
            "in": "path",
            "name": "series_id",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/SeriesId"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/WatchlistUpsert"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "ok": true
                },
                "schema": {}
              }
            },
            "description": "Acknowledged"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Add or update a watchlist entry",
        "tags": [
          "me-watchlist"
        ]
      }
    },
    "/v1/me/watchlist/{series_id}/sync": {
      "put": {
        "description": "Set the blanket per-series sync-exclusion flag (design v2 §A.5).\n\nThe flag lives on the watchlist entry, so the series must already be tracked. It answers\n`404` when it is not, rather than the `{\"ok\": true}` it used to answer unconditionally\n(OPS-2.2d): this decides whether the caller's reading progress is pushed to an external\nprovider, and a privacy setting that reports success without persisting is worse than one\nthat refuses.",
        "operationId": "put_sync_excluded",
        "parameters": [
          {
            "description": "Series id",
            "in": "path",
            "name": "series_id",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/SeriesId"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SyncExcluded"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "ok": true
                },
                "schema": {}
              }
            },
            "description": "Acknowledged"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "The caller does not track this series"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Set the per-series sync-exclusion flag",
        "tags": [
          "me-watchlist"
        ]
      }
    },
    "/v1/me/watchlist/{series_id}/sync/{provider}": {
      "put": {
        "description": "Per-provider override of the blanket exclusion flag (design v2 §A.5).",
        "operationId": "put_sync_override",
        "parameters": [
          {
            "description": "Series id",
            "in": "path",
            "name": "series_id",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/SeriesId"
            }
          },
          {
            "description": "Provider slug",
            "in": "path",
            "name": "provider",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SyncExcluded"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "ok": true
                },
                "schema": {}
              }
            },
            "description": "Acknowledged"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "authentication required"
          }
        },
        "security": [
          {
            "bearer_auth": []
          }
        ],
        "summary": "Set a per-provider sync-exclusion override",
        "tags": [
          "me-watchlist"
        ]
      }
    },
    "/v1/providers": {
      "get": {
        "description": "Provider list + per-provider series counts, for the Discover provider filter (frontend\n§9.3). Operator-only fields (config/politeness/health) are not exposed; disabled providers\nare hidden.",
        "operationId": "providers",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/PublicProvider"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Enabled providers"
          }
        },
        "summary": "List public providers",
        "tags": [
          "series"
        ]
      }
    },
    "/v1/series": {
      "get": {
        "description": "Filter/sort/paginate the public series list (frontend §9.1). The body remains a plain\n`SeriesSummary[]`; pagination metadata rides on the `X-Total-Count` (rows matching the\nfilter) and `X-Next-Cursor` (next page index, absent on the last page) headers so existing\narray-decoding clients keep working.",
        "operationId": "list",
        "parameters": [
          {
            "in": "query",
            "name": "query",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "content_type",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "status",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Provider slug the series must have a source on.",
            "in": "query",
            "name": "provider",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Tag slugs the series must all carry.",
            "in": "query",
            "name": "tag",
            "required": false,
            "schema": {
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          {
            "description": "Tag slugs the series must not carry.",
            "in": "query",
            "name": "exclude_tag",
            "required": false,
            "schema": {
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          {
            "in": "query",
            "name": "year_min",
            "required": false,
            "schema": {
              "format": "int32",
              "type": "integer"
            }
          },
          {
            "in": "query",
            "name": "year_max",
            "required": false,
            "schema": {
              "format": "int32",
              "type": "integer"
            }
          },
          {
            "in": "query",
            "name": "min_chapters",
            "required": false,
            "schema": {
              "format": "int32",
              "type": "integer"
            }
          },
          {
            "description": "`updated | title | chapters | sources | year | rating` (default `updated`).",
            "in": "query",
            "name": "sort",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Zero-based page index (alias: `cursor`).",
            "in": "query",
            "name": "page",
            "required": false,
            "schema": {
              "format": "int64",
              "type": "integer"
            }
          },
          {
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "format": "int64",
              "type": "integer"
            }
          },
          {
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "format": "int64",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/SeriesSummary"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Matching series, newest-updated first by default",
            "headers": {
              "X-Next-Cursor": {
                "description": "Next page index; absent on the last page",
                "schema": {
                  "format": "int64",
                  "type": "integer"
                }
              },
              "X-Total-Count": {
                "description": "Total rows matching the filter",
                "schema": {
                  "format": "int64",
                  "type": "integer"
                }
              }
            }
          }
        },
        "summary": "Browse the catalogue",
        "tags": [
          "series"
        ]
      }
    },
    "/v1/series/{id}": {
      "get": {
        "operationId": "detail",
        "parameters": [
          {
            "description": "Series id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/SeriesId"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SeriesDetail"
                }
              }
            },
            "description": "Series detail"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Series not found"
          }
        },
        "summary": "Get series detail",
        "tags": [
          "series"
        ]
      }
    },
    "/v1/series/{id}/chapters": {
      "get": {
        "description": "Chapter list, newest first. When a valid access token is supplied the per-chapter `read`\nflag is populated from the user's progress (frontend §9.2 auth-scoped read-state);\nanonymous callers get the same list without read-state.",
        "operationId": "chapters",
        "parameters": [
          {
            "description": "Series id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/SeriesId"
            }
          },
          {
            "description": "Which source to read chapters from. Defaults to the first source of the series.",
            "in": "query",
            "name": "source",
            "required": false,
            "schema": {
              "$ref": "#/components/schemas/SeriesSourceId"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/ChapterDto"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Chapters, newest first"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Series (or its source) not found"
          }
        },
        "security": [
          {},
          {
            "bearer_auth": []
          }
        ],
        "summary": "List a source's chapters",
        "tags": [
          "series"
        ]
      }
    },
    "/v1/series/{id}/similar": {
      "get": {
        "description": "Content-similar series, ranked by an approximate nearest-neighbour search over the\nrecommendation model's embedding space, with the features each match shares with the seed.\n\nFalls back to the catalogue's popularity prior when the seed has no embedding yet — a series\nadded since the last model build, or a deployment that has never run one. An empty array\nmeans the model has never been built at all.",
        "operationId": "similar",
        "parameters": [
          {
            "description": "Series id",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "$ref": "#/components/schemas/SeriesId"
            }
          },
          {
            "description": "How many to return (default 12, max 50)",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "format": "int64",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/SimilarSeries"
                  },
                  "type": "array"
                }
              }
            },
            "description": "Similar series, closest first"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            },
            "description": "Series not found"
          }
        },
        "summary": "Get similar series",
        "tags": [
          "series"
        ]
      }
    },
    "/v1/tags": {
      "get": {
        "description": "All genres/tags in the catalogue (public).",
        "operationId": "tags",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "items": {
                    "$ref": "#/components/schemas/Tag"
                  },
                  "type": "array"
                }
              }
            },
            "description": "All known tags"
          }
        },
        "summary": "List all tags",
        "tags": [
          "series"
        ]
      }
    }
  },
  "tags": [
    {
      "description": "Registration, login, session refresh/logout",
      "name": "auth"
    },
    {
      "description": "Public catalogue browse/detail/chapters",
      "name": "series"
    },
    {
      "description": "Operator-published legal documents; unauthenticated",
      "name": "legal"
    },
    {
      "description": "The signed-in user's watchlist",
      "name": "me-watchlist"
    },
    {
      "description": "Per-series reading progress",
      "name": "me-progress"
    },
    {
      "description": "Feed, continue-reading, recommendations, stats",
      "name": "me-dashboard"
    },
    {
      "description": "Profile and active-session management",
      "name": "me-account"
    },
    {
      "description": "In-app notifications and the live SSE stream",
      "name": "me-notifications"
    },
    {
      "description": "External tracker sync (AniList, ...), user-facing",
      "name": "me-sync"
    },
    {
      "description": "Operator provider CRUD, state and health",
      "name": "admin-providers"
    },
    {
      "description": "Scan run history, failures, live scan stream",
      "name": "admin-scans"
    },
    {
      "description": "Series merge-candidate review",
      "name": "admin-matching"
    },
    {
      "description": "Operator visibility into external sync mappings",
      "name": "admin-sync"
    },
    {
      "description": "User directory, identity, suspension and permission grants",
      "name": "admin-users"
    },
    {
      "description": "The GDPR data-subject request queue and its fulfilment",
      "name": "admin-privacy"
    },
    {
      "description": "Runtime feature flags — the deployment control plane",
      "name": "admin-feature-flags"
    },
    {
      "description": "System stats and audit log",
      "name": "admin-overview"
    }
  ]
}